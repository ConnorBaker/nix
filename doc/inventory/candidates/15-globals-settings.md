# Globals, settings architecture, and dependency-injection

Candidates 131-149. Seventeen VALID; #137 and #146 PARTIALLY VALID. The
codebase already documents most of its own debt here — many of the FIXME
comments quoted in the candidates are in source today.

**Keystones:** #136 (split `Setting<T>` self-registration into a two-phase
`Config::registerSettings()`) is the unblocker for #134, #137, #144,
#149 (most of the Settings architecture cluster), and partially for
#19/#43 (the X-macro driver requires plain-member settings; #21 is
independent of #136). **#135 does NOT actually depend on #136** — per
B1's source-level verification, inlining `GCSettings`/`AutoAllocateUidSettings`'s
`Setting<T>` members into `LocalSettings` preserves the registration
target, because `Setting<T>::Setting`'s `addSetting(this)` reaches the
same most-derived `Config` regardless of inheritance shape. #135 can
land independently today. #137 (consolidate `GlobalConfig::Register`
instances) removes the link-order SIOF dependency. #138 (move
`readOnlyMode` out of `EvalSettings`) unblocks #131 (whose
`LookupPathHooks` registry currently goes through #138's pointer alias).
The chain is **#136 → #137 → #134/#144/#149**, plus
**#138 → #131** as a parallel branch. #135 is independent.

**See also:** N3 (eight registries with the `Registry<Key, Value>`
shape including `Setting<T>` self-registration #136 and
`GlobalConfig::Register` #137 as the seventh and eighth), N4 (X-macro
driver for the 21-22 `*Settings` structs — alternative keystone
replacing the chain #136 → #19), N17 (the leaked-Sync<T*> pattern
recurs in #147, #148, and `getInterruptCallbacks()`), N18 (env-var-only
configuration iceberg above the test surface of #146).

| # | Verdict | Effort |
| - | ------- | ------ |
| 131 | VALID | small |
| 132 | VALID | trivial (decomposition) / large (full DI) |
| 133 | VALID | small |
| 134 | VALID | medium (gated by #136) |
| 135 | VALID | small |
| 136 | VALID | large to structural |
| 137 | PARTIALLY VALID | small (after #136) / medium (without) |
| 138 | VALID | medium |
| 139 | VALID | medium |
| 140 | VALID | trivial |
| 141 | VALID | small (per command) / medium (full handler-context) |
| 142 | VALID | trivial |
| 143 | VALID | structural |
| 144 | VALID | medium (collapse small ones) / structural (full) |
| 145 | VALID | medium |
| 146 | PARTIALLY VALID | small (boolean subset) / medium (full) |
| 147 | PARTIALLY VALID | medium |
| 148 | INVALID (closed by adversarial review) | none |
| 149 | VALID | structural |

---

131. **`evalSettings` is constructed at static-init time with a literal `LookupPathHooks` table that hard-wires it to `settings.readOnlyMode`.** The global in `libcmd/common-eval-args.cc` is initialised with a hard-coded `LookupPathHooks` table that calls `parseFlakeRef`, `fetchToStore`, and `state.allowPath`; it also takes `&settings.readOnlyMode` (from `libstore/include/nix/store/globals.hh`) by reference, hard-wiring two globals together. The `LookupPathHooks` registry should live on `EvalState` (added imperatively in `initNix` or by the embedder), and `EvalSettings::readOnlyMode` should query the active `Store`/`StoreConfig` rather than alias another global.
    - ../verified/15-libflake-libmain.md, ../verified/16-libcmd.md
    - **Validation:** VALID. `LookupPathHooks` is a `std::map<std::string, fun<LookupPathHook>>` member of `EvalSettings`; the `EvalSettings` constructor takes both `readOnlyMode` and `lookupPathHooks` as arguments — so the wiring is structural (cannot be constructed without both). Move to `EvalState::registerLookupPathHook(scheme, fun)` called from `initNix` or embedder setup. Compounds with #133. **Sequencing:** depends on #138 landing first, since #131's `readOnlyMode` reference in the `EvalSettings` ctor goes through #138's pointer alias; removing the alias means #131's wiring requires a new source for `readOnlyMode`. Effort: small.

132. **Cross-library settings globals are constructed in libcmd, not in the libraries that own the types.** `libcmd/common-eval-args.cc` defines `fetchSettings` (libfetchers), `evalSettings` (libexpr), `flakeSettings` (libflake), and `compatibilitySettings` (libcmd) as namespace-scope globals with their own `GlobalConfig::Register` lines. The owning libraries declare these as `extern`, but the storage and the registration both live in libcmd, forcing every libstore-only consumer to pull in libcmd just to link against the globals. Each library should own its own singleton (or, better, none at all — pass via context).
    - ../verified/16-libcmd.md
    - **Validation:** VALID. All four `extern` declarations live in `libcmd/include/nix/cmd/common-eval-args.hh`, **not** in their owning libraries' headers. Each owning header carries a `@todo Get rid of global settings variables` comment. Two paths: (a) each library owns its own singleton (mechanical, preserves global model) — trivial; (b) full DI via constructor injection — large/structural. **Pitfall:** `evalSettings` initialiser captures `settings.readOnlyMode` (#138) and registers a flake lookup hook (#131); moving requires resolving those entanglements first. Effort: trivial (decomposition only) / large (full DI).

133. **`flake::Settings::configureEvalSettings` is plugin registration smuggled into a `configure*` method.** The method in `libflake/settings.cc` (called from `nix/main.cc`) imperatively appends three primops to `evalSettings.extraPrimOps` — that is, a method on a settings type imperatively pushes entries into another settings type's vector. The right shape is an `EvalState::registerPrimOp(PrimOp)` call at process init, with libflake providing a `flake::registerPrimOps(EvalState &)` free function — no settings type required.
    - ../verified/15-libflake-libmain.md
    - **Validation:** VALID. The method takes a non-const `EvalSettings &` and pushes into the public `extraPrimOps` field — its name is misleading (configures nothing about settings). **Pitfall:** `primops::getFlake` captures `flake::Settings` by reference into the primop's function closure; moving the `flake::Settings` to per-EvalState requires the closure to capture from there too. With #131 done, `EvalSettings` ctor reduces to nothing extra. Effort: small.

134. **`Settings` privately inherits from four sibling settings groups via `: public virtual Config` only to namespace fields.** `Settings` in `libstore/include/nix/store/globals.hh` is a virtual-base diamond of `LocalSettings`, `LogFileSettings`, `WorkerSettings`, `NarInfoDiskCacheSettings`, exposed via `getLocalSettings()` / `getLogFileSettings()` / `getWorkerSettings()` / `getNarInfoDiskCacheSettings()` accessors that all just `return *this`. The inheritance has no behavioural purpose — it is purely a way to namespace fields and inherit `addSetting` registration through `Config`. The header even apologises for it ("we expose them from `settings` with `settings.getLocalSettings()` for now ... we would much prefer if you use [`localStore.config->getLocalSettings()`]"). Composition (a `Settings` that *holds* the four sub-configs and registers them via `addSetting` forwarding) would be both clearer and would let the per-store version of `LocalSettings` actually decouple.
    - ../verified/07-libstore-local.md
    - **Validation:** VALID. **Zero `dynamic_cast<LocalSettings*>` sites anywhere in the tree** — every consumer goes through `getLocalSettings()` etc. So the composition refactor is unblocked at the consumer side; only #136's constructor self-registration blocks it on the implementation side. **#134 genuinely depends on #136** (per B1's source-level verification): the virtual-base diamond is exactly what makes the `Setting<T>{this, ...}` self-registration land on the most-derived `Settings::_settings`; replacing inheritance with composition either needs #136 (a `registerSettings()` mechanism) or splits the registration into multiple `globalConfig::Register` lines, which is a behavioural change. **Asymmetry with #135:** B1 also verified that #135 (the `LocalSettings` flattening, see below) does NOT depend on #136 — that's the difference. **Pitfall:** external code (Hydra-like consumers) using `settings.someLocalField` directly — ABI breaks on the type but field syntax is preserved if accessors stay. **C-API readers (per V1):** `nix_api_store.cc` reads `nix::settings.readOnlyMode` directly via field syntax. The `Settings::readOnlyMode` field is on `Settings` directly (not on a diamond base), so #134's composition refactor leaves the field path intact. V1 also found that 5 of 6 C-API libraries `install_headers` with their `*_internal.h(h)` files, exposing some layout to C++ consumers — verify the `Settings` struct itself is not in any installed `*_internal.h(h)`; it is not, but the layout-stability concern still applies if relocation moves fields between parent and child types. **Sequencing:** compounds with #144 (do first); after #144 collapses `LogFileSettings`/`NarInfoDiskCacheSettings`/etc. into `Settings` directly, the diamond shrinks to just the `LocalSettings`-vs-`Settings` relationship — much narrower. **Consider:** folding into #144 as an implementation step rather than a separate candidate; kept separate here because the dependency on #136 is the load-bearing fact. **See also:** B1, V1, E2. Effort: medium (gated by #136; structural otherwise).

135. **`LocalSettings` multiply-inherits `GCSettings` and `AutoAllocateUidSettings` only for grouping in docs.** `GCSettings` and `AutoAllocateUidSettings` (`libstore/include/nix/store/local-settings.hh`) both `: public virtual Config` and contribute their own `Setting<T>{this, ...}` definitions; `LocalSettings` then multiply-inherits both. There's no polymorphism, no overrides, no methods — they're just struct namespaces. A flat `LocalSettings` with `gc_*`/`autoAlloc_*` field-name prefixes would be simpler. The same critique applies to `LogFileSettings` and `NarInfoDiskCacheSettings` in `globals.hh`.
    - ../verified/07-libstore-local.md
    - **Validation:** VALID. **Pitfall:** `getAutoAllocateUidSettings()` returns `nullptr` when `autoAllocateUids` is off, used by callers as a presence check; flattening would require those callers to read `autoAllocateUids` directly. Two options: (a) flatten — drop `GCSettings` and `AutoAllocateUidSettings`, inline their `Setting<T>` members into `LocalSettings` directly with `gc-`/`auto-allocate-` name prefixes (settings already have these prefixes in their `name` parameter); (b) composition (gated on #136). (a) is trivial after consumers stop calling `getGCSettings()`. Compounds with #134. **Independence from #136:** B1 verified by reading the source that #135 does NOT depend on #136. Inlining `GCSettings`/`AutoAllocateUidSettings`'s `Setting<T>` members into `LocalSettings` preserves the registration target, because `Setting<T>::Setting`'s `addSetting(this)` reaches the same most-derived `Config` regardless of inheritance shape. The catalog's previous keystone-block claim that #135 was blocked by #136 was wrong; it can land independently today. **See also:** B1 for the source-level verification. Effort: small.
    - **Branch:** `vibe-coding/cleanup/libstore` (option (a), flatten. `GCSettings` and `AutoAllocateUidSettings` virtual base structs dropped; their `Setting<T>` members inlined into `LocalSettings` with `gc-`/`min-free`/`max-free`/`start-id`/`id-count` prefixes preserved via the existing `name` parameter on each `Setting`. All consumers migrated: `LocalStore::collectGarbage`, `LocalStore::autoGC`, `local-store.cc` reservedSize, `nix-store.cc` opQueryReferrers, `build/worker.cc` autoGC poll, and `acquireUserLock`'s presence check (now reads `localSettings.autoAllocateUids` directly). `AutoUserLock::acquire` now takes a single `UidRange` struct (file-local in `unix/user-lock.cc`, holding `startId` and `count`) instead of `const AutoAllocateUidSettings &`; the struct bundling guards against silent argument-order swaps that would have been possible with two adjacent `uint32_t` parameters. Re-derives B1's source-level verification of #135's independence from #136.).

136. **`Setting<T>{this, ...}` constructor sneakily registers itself with `*this` config via `options->addSetting(this)`.** Each `Setting` member's constructor (`libutil/include/nix/util/configuration.hh`) writes a pointer to itself into the parent `Config::_settings` map. This makes `Setting<T>` non-trivially-relocatable, forbids copying or moving a `Config`, and forces the canonical heavy `Setting<T>{this, default, name, R"(doc)", aliases, xpFeature}` constructor invocation on every line. A two-phase approach — `Setting<T>` is a plain member, then a `void registerSettings()` method or a constexpr table iterates them at the end of `Config`'s constructor — would let settings be tabulated in a single static array per config (compounds with #19 and #21).
    - ../verified/03-libutil-runtime.md
    - **Validation:** VALID. **Keystone refactor.** Makes both `Setting<T>` and `Config` non-relocatable, non-movable, non-copyable: any moved-from `Config` has stale pointers in `_settings`. The specialisation `Setting<AbsolutePath>` does the same; `DeprecatedWarnSetting`/`PluginFilesSetting` do `options->addSetting(this)` manually. **Pitfall:** every existing `Setting` site (several hundred) gives `(this, default, name, doc, aliases, doc-default, xpFeature)` — flipping to a static descriptor list requires rewriting all of them or providing a `static constexpr` member-pointer-keyed view. Cross-cuts unblock #134, #135, #137, #149 (and likely #19/#21). Effort: large to structural.

137. **`GlobalConfig::Register` instances are scattered across 13 TUs (15 production register sites + 1 test-only site = 16 total) and rely on link-order static-init.** Every settings struct gets a file-scope `static GlobalConfig::Register r…(&config);` instance. The production sites: `globals.cc`, `filetransfer.cc`, `archive.cc`, `fs-sink.cc`, `logging.cc`, `config-global.cc` (`rSettings` for `experimentalFeatureSettings`), four sites in `common-eval-args.cc` (`rFetchSettings`, `rEvalSettings`, `rFlakeSettings`, `rCompatibilitySettings`), `plugin.cc`, `develop.cc`, `nix-env.cc`, `unix/daemon.cc`, `upgrade-nix.cc`. Plus one test-only site in `libutil-tests/nix_api_util.cc`. Consolidating into an explicit `registerBuiltinConfigs()` call inside `initLibStore()`/`initLibUtil()`/`initNix()` (similar to how `initLibStore` already calls `loadConfFile(globalConfig)`) would make startup deterministic and let the order be documented.
    - ../verified/03-libutil-runtime.md, ../verified/15-libflake-libmain.md
    - **Validation:** PARTIALLY VALID. **Counts:** 16 lines total = 15 production + 1 test (verified `grep -rEn 'GlobalConfig::Register ' src/` returns 16). SIOF analysis: `configRegistrations()` is a Meyers singleton (function-local `static`), so the deque itself is SIOF-safe within one TU; the actual SIOF risk is link-order between TUs determining push order. **Pitfall:** `experimentalFeatureSettings` is registered before `loadConfFile` because `loadConfFile` consults the registry; ordering inside the explicit init function must respect that. **Init-sequencing constraint:** the explicit init must precede any setting access, including those from `loadConfFile` and from the C-API entry points (`nix_setting_get`/`nix_setting_set` in `nix_api_util.cc`); auditing this sequencing is part of the small/medium effort, not optional. **Test-site impact:** the test-only `GlobalConfig::Register rs(&mySettings);` in `libutil-tests/nix_api_util.cc` will need rewriting (or to call the explicit registration too) once the static-init shape is deprecated. **See also:** N3 (eight registries with the same skeleton — this is one of them), N4 (X-macro driver that would generate the registration too), #57 (`LENGTH_PREFIXED_PROTO_HELPER_X` macro hygiene normalised in the same `libstore-dead-decls` branch). Effort: small (after #136); medium (without #136).

138. **`settings.readOnlyMode` is plumbed into `EvalSettings` via a `bool *` reference field.** `EvalSettings::readOnlyMode` is a raw `bool *` aliasing `settings.readOnlyMode` (`libexpr/include/nix/expr/eval-settings.hh`, set up in `libcmd/common-eval-args.cc`); the comment says `FIXME: This really shouldn't be public. The C API should have non-global settings instead.`. The pointer aliasing exists solely to bridge two separate globals; if `readOnlyMode` lived on `Store`/`StoreConfig` (which actually decides whether writes happen) and `EvalSettings` queried it via the `EvalState`'s store, the alias and the FIXME both vanish.
    - ../verified/11-libexpr-eval.md
    - **Validation:** VALID. Twelve+ mutation sites flip `settings.readOnlyMode = true;` (`nix/flake.cc`, `search.cc`, `log.cc`, `derivation-add.cc`, `nix-env.cc`, `nix-instantiate.cc`, `libcmd/installables.cc`, `libcmd/repl.cc`, `nix-c/nix_api_store.cc`, etc.). Move `readOnlyMode` to `StoreConfig` (so `dummy://` and similar already implicitly enable it), have callers query `state.store->config->readOnly()`. **Pitfall — two C-API readers:** `nix_api_store.cc` reads `nix::settings.readOnlyMode` directly via field syntax, and `nix_api_expr.cc` does a *pointer alias* (`builder->settings.readOnlyMode = &nix::settings.readOnlyMode;`) into a field of `EvalSettings`, which is exactly the pointer this candidate wants to remove. The removal must (a) keep the field in `EvalSettings` for one release as a deprecated shim (always-true alias), (b) update `nix_api_store.cc` to query `store->config->readOnly()` or equivalent, and (c) update `nix_api_expr.cc` to either alias the new `StoreConfig::readOnly()` accessor or stop aliasing at all. Compounds with #131 (whose `EvalSettings` ctor goes through #138's pointer alias — #131 depends on #138 landing first). **Latent UAF (per F1+V3, code-trace confirmed):** `nix_eval_state_builder_new` allocates a heap `bool` via `nix::make_ref<bool>(true)`; the builder's `nix::ref<bool> readOnlyMode` is the **sole** owner; `EvalSettings::EvalSettings(bool &)` stores `&readOnlyMode` in the raw `bool *` member; `nix_eval_state_build` does NOT transfer the `nix::ref<bool>` into the resulting `EvalState`; `nix_eval_state_builder_free` destroys the only `nix::ref<bool>`, freeing the heap cell; post-`_free`, the `EvalState`'s `EvalSettings::readOnlyMode` is dangling and is dereferenced in `EvalState::ensureLazyPathCopied` and the `addToStore`/`fetchToStore` paths in `eval.cc` via `settings.isReadOnly()`. Reachable from documented C-API usage (the `_new`-without-`_load` path is documented as supported in `rl-2.28.md` and exercised by 4 in-tree tests). The `_load` path is safe because it rebinds the pointer to `&nix::settings.readOnlyMode` (program-lifetime). **The right fix folds the UAF repair into this candidate's `readOnlyMode → Store` migration**: deleting the `bool *` field on `EvalSettings` removes the dangling pointer entirely. **C++ ABI caveat (per V1):** deleting the dead `nix::ref<bool> readOnlyMode` field in `nix_eval_state_builder` is a layout change to the publicly-installed `nix_api_expr_internal.h` (5 of 6 C-API libraries `install_headers` with their internal headers; opacity holds for C consumers but layout is exposed to C++ consumers). Call out in release notes. **See also:** E2, V1, F1, V3. Effort: medium (was: medium).

139. **`drvHashes` is a process-wide concurrent memo table outliving any `Store`.** `DrvHashes drvHashes;` in `libstore/derivations.cc` is a `boost::unordered::concurrent_flat_map<StorePath, DrvHashModulo>` keyed by store path but globally shared across stores. `pathDerivationModulo(Store & store, ...)` reads the cache without checking whether the cached entry came from a different store with a different `storeDir` — a latent correctness bug as well as a state-leakage problem. Move the memo onto `Store` (next to other per-store caches) so two stores with different `storeDir`s cannot poison each other.
    - ../verified/06-libstore-derivations.md
    - **Validation:** VALID. The header already carries `// FIXME: global, though at least thread-safe.` The cache is also written from `libexpr/primops.cc` (`builtins.derivation` evaluation) at `drvHashes.insert_or_assign`. **Pitfall:** the `libexpr` writer in `builtins.derivation` populates an entry from a derivation just constructed in-memory, *before* any `Store` write — that drv may be materialised later in a different store via `state.store` vs `state.buildStore`. **Per-EvalState (rather than per-Store) may be the more honest scope.** Effort: medium.

140. **`impureOutputHash` is dead namespace-scope code; no caller anywhere in the tree.** `libstore/derivations.cc` defines `const Hash impureOutputHash = hashString(HashAlgorithm::SHA256, "impure");` at file-scope construction time. Originally framed as a SIOF hazard (depends on libsodium's init order); deeper inspection shows the variable has zero call sites — `grep -rn impureOutputHash` across `src/` and `tests/` returns only the definition site, no `extern Hash impureOutputHash` exists in any header, and no tests reference it. The right action is **plain deletion**, not Meyers-singleton wrapping. Belongs squarely in section 7 (dead/stale code); kept here because the original framing was SIOF-flavoured.
    - ../verified/06-libstore-derivations.md
    - **Validation:** VALID. **Reframed:** dead code, not SIOF. The proposed fix in the original body ("static const inside the function that uses it") is moot because there is no function that uses it. **Cross-class:** also a "Latent bug hiding inside duplication" since an unused symbol that *looks* like a hash constant for impure derivations is a genuine source of confusion; deleting it forecloses on someone using it later assuming it has real semantics. **E1 verification cleared:** the symbol has internal linkage (verified via `nm -m` on the built dylib — the `L` after `3nix` in `__ZN3nixL16impureOutputHashE` is the Itanium-ABI internal-linkage marker; this is a C++ language rule per `[basic.link]/3.2` for `const` namespace-scope variables); zero in-tree readers; zero references in lix or Hydra; commit `50912d02e` ("Get rid of `impureOutputHash`", 2022-03-31) intended full removal but missed the namespace-scope definition. Safe to delete as a standalone PR — no deprecation cycle, no downstream coordination. **See also:** E1. Effort: trivial.
    - **Branch:** queued — additional commit on `vibe-coding/cleanup/libstore`.

141. **Legacy `nix-store`/`nix-instantiate`/`nix-channel`/`nix-collect-garbage` keep operation-loop state in file-scope statics.** `nix/nix-store/nix-store.cc` has `static std::shared_ptr<Store> store; static std::filesystem::path gcRoot; static int rootNr; static bool noOutput;`; `nix/nix-instantiate/nix-instantiate.cc` has `gcRoot` (external linkage) and `static int rootNr`; `nix/nix-channel/nix-channel.cc` has `static channels`/`channelsList`/`profile`/`nixDefExpr`; `nix/nix-collect-garbage/nix-collect-garbage.cc` has `deleteOlderThan`/`dryRun`. Every `op*(Strings opFlags, Strings opArgs)` reads from these directly. Threading a context struct through `OpHandler` (compounds with #90 / #91) would eliminate the file-scope globals and make handlers re-entrant for embedders.
    - ../verified/18-nix-modern-2-legacy.md
    - **Validation:** VALID. Compounds with #90, #91. **Latent bug:** only `nix-store` and `nix-channel` use `static`; the others (`nix-instantiate.cc`, `nix-collect-garbage.cc`) have **external linkage** with no `extern` declaration anywhere — accidentally global. Each TU only reads its own file-scope variable today, so a quick fix is to `static`-ify them today even before the larger handler-context refactor. Effort: small (per command, just `static`-ify) / medium (full handler-context refactor).
    - **Branch:** `vibe-coding/cleanup/nix-cli` (per-command `static`-ify subset only: `nix-collect-garbage.cc`'s `deleteOlderThan`/`dryRun` got `static`. The candidate's `nix-instantiate.cc` `gcRoot`/`rootNr` targets had already been folded into the file-scope `static GcRootNamer gcRootNamer;` introduced earlier on this branch in `43a4225b6` (#142's vicinity), so no edit was needed there. The full handler-context refactor remains as future work.)

142. **`chrootHelperName` is a non-`const` `std::string` global with a hard-coded sentinel.** `nix/run.cc` defines `std::string chrootHelperName = "__run_in_chroot";`, used to detect whether `argv[0]` matches the helper sentinel. It's never assigned to. Should be a `constexpr std::string_view`. Trivial change, removes a writable global.
    - ../verified/17-nix-modern-1.md
    - **Validation:** VALID. The candidate body bundles three concerns that the landed commit on `cleanup/nix-cli` addressed together: (1) the non-`const` global with hard-coded sentinel (real refactor — globals/settings architecture); (2) the `Strings helperArgs = { chrootHelperName, ... }` brace-init pitfall (build-correctness fix — `std::string`'s constructor from `string_view` is `explicit`, requires `std::string(chrootHelperName)` wrap because `Strings` is `std::list<std::string>`); (3) the `main.cc` `#include "run.hh"` adjustment (build-system fix — the original framing claimed the include was already present; in fact `main.cc` did not include it; the landed PR adds the include under the existing `#ifndef _WIN32` guard). The other readers (`extern` in `main.cc`, the `argv[0] == chrootHelperName` compare) are fine since C++17 defines `operator==` between `const char *` / `std::string` and `string_view`. Cleanest definition is `inline constexpr std::string_view chrootHelperName = "__run_in_chroot";` in `run.hh`. **Cross-class:** also fits "Dead or stale code" (effectively dead-write — never reassigned) and "Latent bugs hiding inside duplication" (the brace-init issue). The branch fix reflects all three. Effort: trivial.
    - **Branch:** `vibe-coding/cleanup/nix-cli`

143. **Logging globals (`verbosity`, `nextId`, `curActivity`, `logger`) live as four orthogonal namespace-scope variables.** `libutil/logging.cc` exposes `Verbosity verbosity = lvlInfo;` (writable, mutated from CLI flag handlers in `common-args.cc` and `nix/main.cc`), `std::atomic<uint64_t> nextId{0};` (Activity ID generator), `static thread_local ActivityId curActivity = 0;` (per-thread current activity), and `std::unique_ptr<Logger> logger = makeSimpleLogger(true);` (process-wide sink). `logger` is reassigned from at least eight sites (`libmain/loggers.cc`, `nix/build-remote/build-remote.cc`, `libstore/daemon.cc`, `libstore/unix/build/derivation-builder.cc`, `libstore/unix/build/child.cc`, `libutil/logging.cc`, `libutil/unix/processes.cc`, `libexpr-tests/primops.cc`), often with no synchronisation. The four are one logical "log subsystem" and should live on a `LogSink`/`LoggingContext` that `EvalState`/`Store` hold by reference; at minimum, `verbosity` should be a member of the active `Logger` rather than a separate writable global.
    - ../verified/03-libutil-runtime.md
    - **Validation:** VALID. Eight reassignment sites for `logger` (7 production + 1 test, verified), no synchronisation around the `unique_ptr` swap. **Pitfall:** free functions `printError`/`printInfo`/`printTalkative`/`warn`/`notice` (in `logging.hh`) call `logger->log(...)` with the global pointer; converting them to take `LoggingContext &` requires touching essentially every printf-style log call (hundreds of sites). A staged migration via TLS-stack (`thread_local LoggingContext * activeContext`) lets free functions keep working while explicit `state.log.warn(...)` becomes the long-term API. **TLS-fallback shutdown-ordering hazard:** `thread_local` variables in non-detached threads are destroyed when the thread exits; the main thread's TLS is destroyed *before* namespace-scope statics. So if a free function (e.g. `printError`) fires from a static destructor, the TLS-stack would be empty and the fallback to the global `logger` would activate. Free functions must gracefully handle "TLS-stack empty" (the current `logger` global fallback), not assert non-empty — don't introduce a new shutdown-order failure mode while migrating away from the existing shutdown-order coupling. Compounds with #144 (`LoggerSettings` becomes a sub-config). **See also:** N17 (the leaked-Sync<T*> SIOF pattern), Cluster A (concurrent state and globals), A4 (`LoggingContext` architectural theme). Effort: structural.

144. **The settings ecosystem is 19 separate `Config`-derived globals.** `Settings`, `LocalSettings`, `WorkerSettings`, `LogFileSettings`, `NarInfoDiskCacheSettings`, `EvalSettings`, `fetchers::Settings`, `flake::Settings`, `CompatibilitySettings`, `FileTransferSettings`, `ArchiveSettings`, `RestoreSinkSettings`, `LoggerSettings`, `PluginSettings`, `DevelopSettings`, `EnvSettings`, `AuthorizationSettings`, `UpgradeSettings`, `ExperimentalFeatureSettings`. Some splits are meaningful (eval vs store vs fetchers), some are not (`LogFileSettings`, `NarInfoDiskCacheSettings`, `WorkerSettings` exist only to fit into the diamond inheritance described in #134; `CompatibilitySettings` has two boolean fields and lives in libcmd; `LoggerSettings` has three fields). The long-term direction is a per-subsystem config object owned by the corresponding subsystem object (`Store::config`, `EvalState::config`, etc.) — not a global registry.
    - ../verified/03-libutil-runtime.md, ../verified/07-libstore-local.md, ../verified/11-libexpr-eval.md, ../verified/15-libflake-libmain.md, ../verified/16-libcmd.md
    - **Validation:** VALID. **Pitfall:** each `Config` has distinct `nix.conf` parsing and documentation generation; merging requires the docgen pass to keep producing the same Markdown headings. Quick incremental wins: collapse `LogFileSettings`/`NarInfoDiskCacheSettings` into `Settings` directly (both pure namespace-flavoured field groups); collapse `CompatibilitySettings` into `MixEvalArgs` or `EvalSettings` (only used by libcmd). Compounds with #136, #137. Effort: medium (collapse the small ones) / structural (full per-subsystem ownership).

145. **`experimentalFeatureSettings.require(Xp::*)`/`.isEnabled(...)` is sprinkled across 73 call sites in 35 files (`.require` alone: 41 sites in 24 files).** Each command, primop, fetcher, and store backend independently calls `experimentalFeatureSettings.require(...)` or `.isEnabled(...)`. There's also `Args::Flag::experimentalFeature`, `AbstractSetting::experimentalFeature`, `InputScheme::experimentalFeature()`, `Command::experimentalFeature()`, and the per-flake `Xp::Flakes` checks in `parseFlakeRef`. A single feature-gate decorator at the dispatch boundary (commands, primops, input schemes, store builders) would consolidate them. `nix/main.cc` already does `experimentalFeatureSettings.require(args.command->second->experimentalFeature())` for top-level commands; extending the same idea to primops/fetchers/store-types would replace the scattered in-body `.require` calls.
    - ../verified/03-libutil-runtime.md, ../verified/13-libexpr-primops.md, ../verified/14-libfetchers.md
    - **Validation:** VALID — counts confirmed exactly (73 `.require`/`.isEnabled` total in 35 files; 41 `.require` alone in 24 files). Existing decorators that already use the experimental-feature machinery declaratively: `Args::Flag::experimentalFeature`, `AbstractSetting::experimentalFeature`, `InputScheme::experimentalFeature()` virtual, `Command::experimentalFeature()` virtual. `nix/main.cc` already does `experimentalFeatureSettings.require(args.command->second->experimentalFeature())` at top-level dispatch. Add `PrimOp::experimentalFeature` (`std::optional<ExperimentalFeature>`); the work is calling the existing virtuals at one place per dispatch boundary rather than ad-hoc throughout the body. Effort: medium.

146. **`_NIX_TEST_*`/`_NIX_FORCE_*` test environment variables form an ad-hoc test-config protocol scattered across 14 distinct names in `src/` (20 tree-wide including `tests/`).** `_NIX_FORCE_HTTP` (×3: `git.cc`, `http-binary-cache-store.cc`, `local-binary-cache-store.cc`), `_NIX_TEST_NO_LSOF`, `_NIX_TEST_NO_ENVIRONMENT_WARNINGS`, `_NIX_TEST_REPL_ECHO`, `_NIX_TEST_FREE_SPACE_FILE`, `_NIX_TEST_BARF_ON_UNCACHEABLE`, `_NIX_TEST_CONCURRENT_FETCHES`, `_NIX_TEST_CONCURRENT_SUBSTITUTION`, `_NIX_TEST_RAW_MARKDOWN`, `_NIX_TEST_NO_SANDBOX`, `_NIX_TEST_GC_SYNC_1`/`_NIX_TEST_GC_SYNC_2`, `_NIX_TEST_ACCEPT`, `_NIX_TEST_UNIT_DATA` (used by every libxxx-tests build to point at the `data/` directory; consumed by `libutil-test-support/include/nix/util/tests/test-data.hh` and the per-test-suite `meson.build`/`package.nix`), `_NIX_EVAL_SHOW_UNKNOWN_LOCATIONS`. Most are read once into a function-local `static bool`. A single `TestHooks` struct (or a `nix::testing::Settings` that bypasses the user-facing `globalConfig` registry) read once at `initLibStore` would centralise them and make the test surface explicit; the `_NIX_FORCE_HTTP` triplicate is the most obvious immediate target.
    - ../verified/03-libutil-runtime.md, ../verified/07-libstore-local.md, ../verified/14-libfetchers.md
    - **Validation:** PARTIALLY VALID. **Methodology:** scoped to `src/`, the count is 14 distinct names; tree-wide (including `tests/`) the count rises to 20 (`_NIX_TEST_BUILD_DIR`, `_NIX_TEST_CLIENT_VERSION`, `_NIX_TEST_DAEMON_PACKAGE`, `_NIX_TEST_DAEMON_PID`, `_NIX_TEST_SHARED`, `_NIX_TEST_SOURCE_DIR` appear only in `tests/`). The catalog text scopes to `src/` C++ consolidation. The names are real but not all equivalent: `_NIX_TEST_GC_SYNC_1`/`_2` are file-path semaphores (not booleans); `_NIX_TEST_FREE_SPACE_FILE` is a path; `_NIX_TEST_UNIT_DATA` is a path consumed by Meson at build time and by C++ at runtime; `_NIX_TEST_ACCEPT` is read from `libutil-test-support/.../characterization.hh` outside `initLibStore`'s scope (and is read at *every* test invocation, not into a function-local `static bool`). Realistic consolidation: the boolean/flag subset (~10 vars) into a single `TestHooks` struct read once at `initLibStore`. Good first cut: the `_NIX_FORCE_HTTP` triplicate. **See also:** N18 (this is the test-side half of a wider env-var-only configuration iceberg; the production side has 14+ additional `NIX_*` knobs that should be documented), N28 (build-side `_NIX_TEST_UNIT_DATA` plumbing duplicated across 7 test suites). Effort: small (boolean subset) / medium (full consolidation crossing test-vs-production boundaries).

147. **`getFileTransfer()` is a leaked process-wide singleton that auto-recreates a `curlFileTransfer` if the previous one quit.** `libstore/filetransfer.cc` keeps `static auto * const _fileTransfer = new Sync<std::shared_ptr<curlFileTransfer>>;` (intentionally leaked) and the public `getFileTransfer()` regenerates the contents if the worker thread quit. Fine for the `nix` CLI; problematic for embedders (the curl thread can't tear down deterministically, and multiple Stores can't have independent transfer policies). Move ownership to `Store` (or to a curl pool owned by an explicit `IOContext`).
    - ../verified/08-libstore-remote.md
    - **Validation:** PARTIALLY VALID. The header already exposes `ref<FileTransfer> makeFileTransfer(const FileTransferSettings & = fileTransferSettings)` constructing a fresh instance — infrastructure for per-store transfer pools already exists; the singleton is just the default for `HttpBinaryCacheStore(ref<Config>, ref<FileTransfer> = getFileTransfer())`. The default-argument plumbing is already in place. **Pitfall (load-bearing):** the leak of `_fileTransfer` ensures the curl worker thread continues until process exit. If we move the singleton's lifetime to a per-`Store` reference, then a `Store` going out of scope at process shutdown will run `~curlFileTransfer`, which calls `stopWorkerThread()` and then `workerThread.join()` — and during static destruction the join would be called from an arbitrary thread at an arbitrary point in destruction order. The current leak is deliberately load-bearing for this reason. **Recommendation:** the per-Store path is straightforward for new embedders, but the legacy `getFileTransfer()` callers must keep the leak (or use the same `LeakedSingleton<T>` helper proposed by N17). The auto-restart-on-quit behaviour (overwrite with `makeCurlFileTransfer().get_ptr()` if the curl thread quit) is load-bearing for some existing flows; a per-Store pool needs the same restart behaviour or a way to surface "transfer thread is gone" to the caller. **See also:** N17 (the leaked-Sync<T*> pattern recurs three times — `_fileTransfer`, `windowSize`, `_interruptCallbacks`; same fix direction). Effort: medium.

148. **`windowSize` in `terminal.cc` is intentionally leaked for SIOF.** The leak is a workaround per the comment ("called by `getWindowSize()` after `windowSize` has been destroyed"). A function-local static `Sync<...>` would have the same lifetime semantics with no leak and no exported global symbol. Trivial change.
    - ../verified/04-libutil-misc.md
    - **Validation:** **INVALID — closed by adversarial review.** Originally classified as a trivial cleanup; the implementation was attempted on `cleanup/libutil` and reverted after an adversarial review pass identified a runtime hazard. The function-local form does **not** have the same lifetime semantics: the namespace-scope `unique_ptr<Logger>` is initialised before `main`, the function-local `instance` on first call (after `main`); C++ destroys statics in reverse init order, so `instance` is destroyed before `~logger` runs. `~logger` -> `~ProgressBar` -> `stop()` joins the update thread, but the in-flight `draw()` iteration that triggered the join would call `getWindowSize()` -> `windowSize().lock()` on a destroyed mutex (UAM; surfaces as `mutex lock failed: Invalid argument`). The deliberate leak keeps the mutex alive past those destructors. **Recommendation:** keep the leak; close the inventory entry. Same hazard class documented in `cleanup/libutil`'s expanded comment on `getInterruptCallbacks()`. Effort: none.
    - **Branch:** none — attempt-and-revert documented in the second paragraph of `cleanup/libutil`'s primary commit message.

149. **`Config::_settings` map values are non-owning `AbstractSetting *` pointers tied to the parent `Setting` member's lifetime.** `Config::_settings` (`libutil/configuration.cc`) is a `map<name, {isAlias, AbstractSetting *}>`; the pointer must outlive the map, which is enforced only by both being members of the same `Config`. Combined with the SIOF hazard from #137, the result is that no `Config` can be safely instantiated in a non-default location, and `Config` is non-movable/non-copyable in practice. Switching to value-stored settings (variant or type-erased holder owned by the `Config`) is a deeper change but unblocks settings-as-values everywhere downstream.
    - ../verified/03-libutil-runtime.md
    - **Validation:** VALID. Compounds with #136 (chain: #149 depends on #136 — with #136 in, #149 becomes "medium" because the holder pattern is then known). **Mechanism:** the `created = 123;` debug field on `AbstractSetting` is a guard against gcc bug 80431 (the comment in `configuration.cc::~AbstractSetting` reads "Check against a gcc miscompilation causing our constructor not to run, https://gcc.gnu.org/bugzilla/show_bug.cgi?id=80431."). The guard catches the gcc bug case where the constructor body isn't emitted as called; value-storage migration sidesteps the gcc bug differently (forces destructor generation per type), but the guard is still valuable as defence-in-depth and should be preserved (or migrated to a matching guard at the new ownership boundary). Switch `Config::_settings` to value storage — `std::map<name, std::variant<Setting<bool>, ...>>` or a type-erased `std::map<name, std::unique_ptr<AbstractSetting>>` where the `Config` owns settings outright and `Setting<T>` "members" become non-owning views. **Pitfall:** the public API (`Setting<T>::operator T()`, `set`, `get`) is exercised everywhere via field syntax (`settings.foo.get()`); any storage rework must preserve that surface. The chain #149 → #136 → #134, #135, #144 means the keystone is #136. Effort: structural (without #136); medium (with #136 landed).
