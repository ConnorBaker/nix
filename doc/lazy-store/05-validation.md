# How this branch is validated

What the checks are for, which suites exist and how each is run, what the differential harness compares against master, which mutations must make which tests fail, what each of the two platforms finds, which measurements stand and by what command, and what is owed. A number here carries its command; a claim without a test that pins it is marked so.

## 1. What is checked

The conditions of `01-specification.md` section 3: **(C1)**, the store after a command equals the reference's up to the renaming the one address induces; **(C1')**, a failing command fails the same way and leaves a store closed under references; **(C2)**, no path of Δ escapes before its object is in the store. The laws of `01` sections 8, 9.9, 9.10 and 9.11: the naming homomorphism (equal names, equal trees), the object hash equal to git's, route independence of the hash, sharing in the object store, the store's relation to its paths, and collection. The checks are of three kinds: the unit and functional suites, which pin each law and each fixed defect by name; the differential harness, which runs master's own functional suite under the reference and under the branch and compares the stores; and the mutation checks, which show that a test discriminates by forcing the change it guards against.

## 2. The suites, one per command line

Meson intersects `--suite` with the tests named on the same line, so a suite and a list of names must never share a command line; every suite below runs on its own.

**Unit tests.** Five binaries, run as `meson test` runs them, with `_NIX_TEST_UNIT_DATA` pointing at the test directory's `data/` (`src/libutil-tests/meson.build`, `src/libstore-tests/meson.build`):

```
build/src/libutil-tests/nix-util-tests
build/src/libstore-tests/nix-store-tests
build/src/libfetchers-tests/nix-fetchers-tests
build/src/libexpr-tests/nix-expr-tests
build/src/libflake-tests/nix-flake-tests
```

The branch's tests live in `src/libutil-tests/{merkle-hash,object-hash-sink,object-hash,git,source-accessor-naming,archive,mounted-source-accessor,union-source-accessor,file-content-address}.cc`, `src/libstore-tests/{local-store,object-hash-local-store,object-hash-path-info,object-hash-nar-info,object-hash-worker-protocol,content-address}.cc` and `src/libstore-tests/derivation/round-trip.cc`, `src/libfetchers-tests/{git-utils,fetch-to-store}.cc`, `src/libexpr-tests/{write-buffer,add-path,nix_api_expr}.cc` and `src/libexpr-tests/value/value.cc`, `src/libflake-tests/lockfile-v8.cc`. What each pins is named in `01` ("The checks the meaning induces" of sections 9.9 and 9.10), `04` (the "Checks" of each component) and `07-defect-ledger.md`'s table.

**Functional tests.**

```
meson test -C build --no-rebuild --suite git-hashing
meson test -C build --no-rebuild --suite ca
meson test -C build --no-rebuild --suite flakes
meson test -C build --no-rebuild object-hash object-store signing gc gc-auto binary-cache repair fixed add path-info export nix-copy-ssh-ng nix-copy-ssh nars optimise-store multiple-outputs fetchTree-file fetchGit fetchGit-subtree-names db-migration cli-characterisation
```

The third line names the main-suite tests the branch touches or that its defects were found by; the whole main suite is what the Linux package build runs (section 5). `object-hash.sh` and `git-hashing/simple-sha1.sh` have interop blocks that run only when `NIX_REFERENCE_BIN` names the `bin` directory of a Nix built from master: an old client against the new daemon and a new client against the old daemon, without and with the old side's `git-hashing` feature; a cache the old Nix wrote read by this one; a path this Nix signed verified by the old binary; a SHA-1 git-addressed path the old Nix made read here and refused on copy. The tests clear the loader environment before running the reference binary; the reference daemon is started with `NIX_REMOTE` empty and stopped by polling and then `SIGKILL`, since it does not always exit on `SIGTERM`.

The branch's own functional tests, and what each pins:

| test | pins |
|---|---|
| `tests/functional/object-store.sh` | sharing at ingestion at the root and two directories down; two inodes across the executable bit; the identifier equals `nix hash path --mode git`'s; a deleted and collected path frees exactly its objects; `--optimise` links nothing on a store this Nix wrote and removes a planted `.links`, as does a whole-store collection; `--verify --check-contents` finds and `--repair` repairs a corrupt blob of a real file and a corrupt tree; the specific delete's note; substitution through the object store |
| `tests/functional/object-hash.sh` | the column's rendering; `nix store migrate` on a planted schema-10 row; the sweep gated while a row is unmigrated and tolerant of an unentered root; `--optimise` writing an older row's column; a modified schema-10 path not migrated and a path whose files are gone answered from the row; a version-1 signature outliving substitution; the interop blocks above |
| `tests/functional/optimise-store.sh` | master's test made true: outputs share as they are built; `--optimise` links nothing; `--auto-optimise-store` accepted with one warning for `true`, silence for `false`, once per top-level process through the build hook, absent from `nix config show`'s key=value dump and present in its JSON and by name |
| `tests/functional/git-hashing/source-addressing.sh` | every door names a source by git's own SHA-256 tree hash; a `sha256` verified by the shim and refused when wrong; `treeHash` finding a present tree from a dead URL; a fresh lock at version 8 with `treeHash` and no `narHash`; a version-7 lock evaluating, refused with a planted wrong hash, rewritten at version 8 by `nix flake update`; substitution pinned by `treeHash`; a binary-cache round trip of git-named objects |
| `tests/functional/git-hashing/bare-root.sh` | every root kind addressed at the hashing, store, builder and evaluator doors; the plain file's identifier equals `git hash-object`'s; the same bytes as a plain file, an executable and a symlink are three paths |
| `tests/functional/git-hashing/object-store-fetch.sh` | a fetched tarball's name is its tree's identifier and its files links into the object store; refetched after deletion and collection it writes its blobs again; a second tarball differing in one file writes one blob |
| `tests/functional/git-hashing/simple-sha1.sh` | the SHA-1 form refused at every creation entry with the remedy; a planted SHA-1 row read, verified and collected; the reference-binary block |
| `tests/functional/git-hashing/simple-sha256.sh`, `fixed.sh` | master's tests: the identifier equals `git rev-parse`'s in a SHA-256 repository; the fixed-output git mode under SHA-256 builds and under SHA-1 is refused at instantiation |
| `tests/functional/mounted-inputs-listed.sh` | the one intended difference of `01` section 5: `builtins.readDir` of the store directory lists mounted inputs; fails on master by design |
| `tests/functional/fetchGit-subtree-names.sh` | the cost property of subtree names: a second revision differing outside `shared/` does not hash `shared/` again; fails on master by design; an edited checkout copied through `builtins.fetchGit`: the path dumps to the checkout's bytes, its clean subtree's files are the committed path's inodes, `--verify --check-contents` finds every object (the second route of `01` §9.10 on a real working directory; through a daemon the one route, the same assertions) |
| `tests/functional/lazy-store-doors.sh` | a deleted `toFile` object recreated as pending and dereferenced by a `path` fetch and by `builtins.exec`, both by literal text |
| `tests/functional/prefetch-url-attr.sh` | `nix-prefetch-url --attr` honours `name`; fails on master by design (the inverted condition, a segmentation fault) |
| `tests/functional/ca/floating-git.sh` | floating content-addressed outputs under `outputHashMode = "git"`: the recorded address is the git method with the hash `nix hash path --mode git` computes; SHA-1 refused |
| `tests/functional/flakes/old-lockfiles.sh` | master's test kept: an unchanged version-7 lock is left byte-identical by `nix flake lock` |
| `tests/functional/flakes/path-input-copies-once.sh` | a `path:` flake outside the store is copied at its first evaluation, not at a second evaluation of the unchanged tree, and again once edited (the name-first door of `01` section 10) |
| `tests/functional/gc-auto.sh` | master's test with three distinct scripts, since three copies of one script share one blob and `max-free` counts the bytes a deletion frees on the disk |

**The pre-commit check.** `maintainers/check-string-kernel.sh` holds the doors of `04` section 1.2: no text accessor of `EvalState` outside `src/libexpr`, `src/libflake` and the tests; every C entry point that receives an evaluator inside `nix_c_boundary`; no exec outside the two helpers of `src/nix/run.cc`. It proves itself on every run by planting five violations in a scratch copy of the tree and requiring each to be reported; a check that never fails on a plant is indistinguishable from an inert one.

**Package builds.** `nix build .#nix-cli` compiles the CLI; a package build proves compilation alone unless the derivation has a check phase: the unit packages' tests run in their `tests.run` derivations (`nix build .#nix-util-tests.tests.run`, and likewise `nix-store-tests`, `nix-fetchers-tests`, `nix-expr-tests`, `nix-flake-tests`), and `nix build .#nix-functional-tests` runs the whole functional suite in its check phase (`tests/functional/package.nix`). Before `nix build`, `git status --short -- src tests` must show no untracked file: the package's source is the tracked tree.

## 3. The differential harness

`tests/functional/differential/` (its `README.md` describes the files). It runs master's functional scripts, unchanged, under two modes and compares the store afterwards: a **snapshot** is `nix path-info --all --json --json-format 2` with `registrationTime` removed and the store directory blanked, the database tables `ValidPaths` (minus `registrationTime`), `Refs`, `DerivationOutputs` and `BuildTraceV3`, the permanent roots, and the store directory listing (which catches unregistered leftovers), for the main store and for every chroot store under the test root; snapshots are taken at the end of each script and, through a hook in the copied `common/functions.sh`, before every `clearStore`. Each script runs as `tests/functional/meson.build` runs it. A **mode** is a Nix package, the tools PATH, an optional `NIX_CONFIG` fragment, an optional client package and whether to run through a daemon (`modes/*.env.in`: three templates that `prepare-tree.sh` fills into `<dest>/modes/` — `reference.env`; `reference-githash.env`, the reference with `git-hashing` enabled; `branch.env`, the reference tree and bindir with the branch's `nix` first on PATH, its store path from `BRANCH_NIX` or one `sed` — the harness README lists the variables).

Five views, from weakest to strongest: the **name multiset** (`name-multiset.py`: the multiset of object names with the hash part stripped; invariant under the hash churn of nondeterministic inputs; strengthens an exclusion, never turns a DIFF into a SAME); the **renamed view** (`renamed-content.py`, `snapshot.py renamed`: one line per valid object with its name, the digest of its tree after the hash part of every embedded store path is masked and a derivation's path-sorted lists re-sorted, its content-addressedness, and its references' and deriver's names — the same objects with the same bytes up to a name-preserving renaming in the same reference graph by name); its **strict** form, which digests build outputs' bytes too; the **loose** snapshot view (build outputs by path and metadata, unregistered entries with the hash masked), which is the verdict for a reference-against-reference run; and the **strict** snapshot view, which keeps everything. Since the one address every source path differs and the `ValidPaths.hash` column is rendered `git:sha256:<hex>` by the branch and `sha256:<hex>` by the reference, so for a branch-against-reference run the verdict is the renamed view, and the loose view reads DIFF on every test that names a source, as it must. Verdicts per test: SAME-PASS, SAME-SKIP, SAME-FAIL (the same failure on both sides, an environment limit), DIFF; unified diffs are kept beside the B-side snapshots.

```
BRANCH_NIX=<branch nix package> prepare-tree.sh <repo>/tests/functional <tmp>/tree <reference nix>/bin <coreutils>/bin <bash> <version> <system>
export HARNESS_TMPDIR=<tmp>
differential.sh <tmp>/tree/modes/reference.env refA <tmp>/tree/modes/reference.env refB main <scripts...>
differential.sh <tmp>/tree/modes/reference-githash.env reference <tmp>/tree/modes/branch.env branch main <scripts...>
summarize.sh <tmp>/out/summary-<A>-vs-<B>-<suite>.tsv
name-multiset.py <tmp>/out <A> <B> <suite>
renamed-content.py <tmp>/out <A> <B> <suite>
```

The reference side of a branch-against-reference run carries `extra-experimental-features = git-hashing`, which master needs to accept git-named paths (`reference-githash.env`); the branch answers that line with a one-line "has been stabilized" warning. Daemon mode (`HARNESS_DAEMON=1` appended to a mode file) names the branch package as the daemon too (`HARNESS_DAEMON_PACKAGE`), because the client forwards its settings and a daemon that does not know one warns on the client's stderr, which `cli-characterisation.sh` compares. `HARNESS_TMPDIR` must be set — the scripts refuse to run without it — to a resolved, short directory: on macOS `/tmp` is a symlink and the resolved path is what the tests see, since the characterisation tests normalise the working directory, and the daemon socket path must stay under the 104-byte limit.

**What it compares against master, and what it excludes by construction.** Run reference against reference first, the harness is stable on 163 of master's 220 scripts across the seven suites (main 133, ca 25, dyn-drv 11, flakes 34, git 3, git-hashing 3, local-overlay-store 11); 34 skip on macOS (the Linux sandbox, overlayfs, mercurial, manpages, `lang-gc`), one fails identically on both sides (`structured-attrs.sh`, whose `nix develop -c bash -c 'test -n "$out"'` sees `out` only inside a Nix build), and 22 differ between any two runs of master and are excluded from (C1), each with its cause verified at the cited line: the wall clock in a derivation or its output (`check`, `ca/build-trace-delete`, `ca/duplicate-realisation-in-closure`); `$RANDOM` in a derivation or written into `flake.nix` (`extra-sandbox-profile`, `flakes/flakes`, `flakes/inputs`); keys generated per run (`fetchGitVerification`); archives packed at run time (`nix-channel`, `fetchTree-file`, `flakes/build-paths`); a flake evaluated from the test home directory, whose `.cache`, `.local` and profile are copied into the source (`suggestions`, `formatter`, `flakes/run`, `flakes/bundle`, `flakes/config`, `flakes/develop`, `flakes/search-root`); a `path` input's `lastModified` in a lock file (`nix-profile`, `flakes/relative-paths-lockfile`, `flakes/non-flake-inputs`); a `.git` directory inside the copied source (`flakes/shebang`); the environment dump of a content-addressed build (`ca/nix-shell`). Seven more differ between any two *binaries* and are excluded from binary-to-binary comparison: `add-scanning`, `recursive`, `ca/recursive` and the four recursive-Nix tests of dyn-drv bake the directory of the `nix` binary into a derivation. `nix-shell` and `impure-derivations` pass under the loose view with `content-differs`: an environment dump records the build's temporary directory. The name-multiset view is NAMES-EQUAL on every excluded test with a snapshot on both sides.

**Reading a branch-against-reference run.** The verdict is the renamed view; the loose view reads DIFF on every test that names a source, as it must. Three master tests assert the flat hash or a version-7 lock (`add`, `structured-attrs`, `flakes/relative-paths`) and read DIFF by design; the branch tree's versions pass under the branch and fail under the reference. `ca/eval-store` differs under the renamed view because a floating content-addressed derivation's output placeholder is a hash of an input derivation's *renamed* path, which no masking of paths can recover — a limitation of the view, stated. `export-graph` and `nix-shell` are equal under the renamed view and differ only under its strict form (a builder writing paths in path order; an environment dump). Every test the branch rewrote or added was run under the reference and fails there, except `lazy-store-doors` and `ca/floating-git`, which laziness does not touch and which pass under both. The summary TSVs are not kept in the tree; a reader who wants figures re-runs the harness and reads `summarize.sh`'s output.

**The (C2) observer.** `c2-observe.py` reads a command's stdout (or stderr, for `trace` and `throw`) from a second process as the command runs and checks each printed store path as its line arrives — registered in the store's database when `NIX_STATE_DIR` names one, else present on the filesystem — and `C2_ROOT=<dir> c2/run-c2.sh <label> <nix package> <mode>` drives it from a fresh private store over thirteen modes: `drvs` (`nix-instantiate`'s derivation lines), `eval`, `evaljson`, `evalraw`, `match`, `split`, `getctx`, `trace`, `throw`, `repl`, and `lazyeval`, `lazyraw`, `lazyjson`, which build a 4000-file git repository and print its `fetchGit` outPath. Its 300-derivation list (`c2/many-drvs.nix`) makes every serialisation longer than a pipe's stdio buffer, so bytes leave before the command ends. The reading is differential: the branch must never report a path absent where the reference reports it present. Run against the reference, the branch reads present=300 absent=0 in the ten derivation modes on both sides, and in the three mounted-input modes reference absent=1, branch present=1 — `emit` copies the mounted input before printing its path, the intended tightening of `01` section 3. `nix-instantiate --eval` is read-only and prints paths that are not in Δ; the observer reads absent on both sides there, identically. What the observer cannot see: a violation whose output is shorter than the pipe buffer (hence 300), and anything but one process's output — the daemon protocol and files, which coercion-before-build and coercion-before-root protect by construction (`04` section 1.2).

**The measurement scripts** (`tests/functional/differential/measure/`): `workloads.sh` generates the `closure`, `fan` and `files` workloads; `measure-writes.sh` counts the buffer's flushes and batch sizes at talkative verbosity; `count-ops.sh` counts the daemon's worker operations from its debug log; `measure-eval-linux.sh` measures peak resident memory of `nix eval --json` and `nix-instantiate` over N derivations, wrapped as a derivation so a Linux builder runs it. Each takes the binary to measure, so a comparison is two runs, the reference's and the branch's (`MEASURE_ROOT` names their scratch directory). Their results are in section 6.

## 4. The mutation checks

A test is evidence only when a forced change makes it fail. Each row below was built, run, and the change reversed by its own edit (a restore by `mv x.bak x` keeps the copy's older mtime, so `ninja` leaves the mutated object in place and the "after" run tests the mutation; the rebuild is checked with `grep -c <file> build.log`). The rows are the discriminators a maintainer re-runs after touching the component. Last rerun in full on macOS at the tree of the six small items (the collector's dead read, the `path:` input's name-first door, the in-memory add's `autoGC()`, the typed git directory entries, the ATerm writer's store-directory refusal, and the memo measurement): every row's forced change was built, run and reverted by its own edit, and each named test failed under it; the compiler rows refused with `static assertion failed due to requirement '!ReadsBytes<nix::Value>'` (the byte accessors made public) and `no matching function for call to 'execvpAfterEvaluation'` (the call without the token), and `check-string-kernel.sh` reported a planted `execvp(` mention in `src/nix/main.cc` with exit 1 besides its own five plants.

| forced change | must fail | does not discriminate, stated |
|---|---|---|
| `GitObjectStore::link` returns `false` (the hit path never links) | `LocalStoreObjectsTest.restoreThroughObjects` (`filesCreated` 5 where 4, 5 where 0, 1 where 0), `oneChangedFileWritesOneFile` (3 where 1), `restoreThroughObjectsStreamsLargeFiles` (3 where 2), `repairDoesNotLinkACorruptBlob` (1 where 0); the second route: `LocalStoreObjectsTest.materialiseFromObjectsLinksEveryFileAndReadsNothing` (`filesCreated` 5 where 0, `reads` 5 where 0, `listings` 3 where 0, `stats` 9 where 1), `materialiseFromObjectsReadsOnlyWhatTheStoreLacks` (`filesCreated` 5 where 1, `reads` 5 where 3), `materialiseFromObjectsFallsBackOnAMissingOrCorruptObject` (`filesCreated` 4 where 0 at each plant), `materialiseAppliesTheCaseHackAsTheRestorerDoes` (4 where 0), `prop_materialised_path_is_the_restored_path` ("Falsifiable after 1 tests"), `FetchToStoreLocalTest.copyIsMadeFromTheObjectStoreWhereTheMemoNamesWhatItHolds` (`reads` 5 where 3; the window case reads `"FIVE"` where `"five"`) | `executableFirst` has no `filesCreated` assertion; every inode assertion still passes, since inode sharing cannot tell a link-before-write from a write-then-link — the counting hooks (`RestoreSinkHooks::regularFileCreated`) are the discriminator; the materialisation's dump-equality assertions pass too, the fallback being the one route |
| `MaterialisingVisitor::known` returns nullopt (the second route never materialises, every copy the one route under the pull driver) | `LocalStoreObjectsTest.materialiseFromObjectsLinksEveryFileAndReadsNothing` (`reads` 5 where 0, `listings` 3 where 0, `links` 1 where 0, `stats` 9 where 1), `materialiseFromObjectsReadsOnlyWhatTheStoreLacks` (`reads` 5 where 3, `listings` 3 where 2), `materialiseFromObjectsFallsBackOnAMissingOrCorruptObject` (`reads` 4 where 3 and 4 where 2, `listings` 3 where 2; the trusted-blob case `reads` 4 where 0), `FetchToStoreLocalTest.copyIsMadeFromTheObjectStoreWhereTheMemoNamesWhatItHolds` (`reads` 5 where 3; the window case reads `"FIVE"` where `"five"`) | the dump, inode and `filesCreated` assertions of the materialisation tests pass, law 2 holding trivially over a route that is the one route — the read counts (`CountingAccessor`, `NamedCountingAccessor`) are the discriminator |
| `GitObjectStore::readVerifiedTree` skips the identifier check (a tree body trusted as read) | `LocalStoreObjectsTest.materialiseFromObjectsFallsBackOnAMissingOrCorruptObject` at the planted body that parses and names the blob of `"one"` for `e/y`: `readFile(dst / "e/y")` `"one"` where `"four"`, the dump unequal, `reads` 0 where 2 — the subtree taken from the misnaming body under the right name.  A body that does not parse is refused by the parser whatever the check (the first plant tried, a flipped mode byte, did not discriminate and is kept as a second case) | |
| `MemoVisitor::known` inserts a pending key for a node the memo is not consulted for (the `consulted` guard removed: a symlink root's `record` then finds a null key and writes no row) | `GitUtilsTest.memoRowsHoldEntryHashesForBareExecutableAndSymlinkRoots` (`row` false at the symlink root) | |
| the NAR parser rethrows the case-hack collision through `msg()` (`badArchive("NAR contains %s", e.msg())`, its earlier form) | `NarTest.hackedNameCollidingWithAnUpperCaseSuffixEntryIsRefused` (the label inside the message: `NAR contains error: file name …`) | |
| the traversal hands the visitor the on-disk (case-hacked) name instead of the tree's | `MerkleHash.unhackNameAndUnhackedEntries`, `ObjectHashSink.case_hack_suffix_is_not_hashed`, `LocalStoreObjectsTest.caseHackSuffixIsNotHashed` | `ObjectHashSink.filter_matches_dumpPath` and `MerkleHash.narSizeEqualsDumpPathLength` carry no hacked name |
| the push adapter delivers the on-disk name instead of `unhackName` of it | `ObjectHashSink.case_hack_suffix_is_not_hashed`, `LocalStoreObjectsTest.caseHackSuffixIsNotHashed` | |
| the traversal visits a directory's entries in reverse order | `ObjectHashSink.complex_tree_matches_reference_and_nar_length`, `ObjectHashSink.prop_both_routes_match_reference_and_nar_length`, `GitTest.nar_accepts_dot_git_and_rejects_dot_names`, `LocalStoreObjectsTest.restoreThroughObjects`, `caseHackSuffixIsNotHashed` — each with `NAR directory is not sorted` | a tree id and a NAR's length are order-independent: the NAR parser is the sort's discriminator, not the hash |
| the copy visitor creates every child at its parent's path | `CopyRecursive.copies_a_tree_under_both_sink_conventions` | |
| the old-form wire writer ignores `lazyNarHash` | `ObjectHashWorkerProto.lazy_nar_hash_is_forced_by_the_old_form_alone`; `object-hash.sh` at the first producer the interop block reaches against the reference daemon | |
| `inputHashMismatch` writes an empty kind | `GitUtilsTest.narHashOfIsTheMemoisedShim`; `git-hashing/source-addressing.sh` and `fetchGit.sh` at their `grepQuiet 'NAR hash mismatch'` | |
| `InputScheme::allowedAttrs()` omits `treeHash` | `LockFile.version8RoundTripsWithTreeHash`; `git-hashing/source-addressing.sh` (`not supported by scheme`) | |
| `GitRepoImpl::getAccessor`, both overloads, leaves the export-ignore filter out unconditionally (the identity short-circuit forced on) | `GitUtilsTest.exportIgnoreDeclaredAnywhereKeepsTheFilter` at every source's `pathExists` of the hidden file (`Actual: true`, nine sources), `GitUtilsTest.exportIgnoreWrapperPassesNamesThroughWhenItHidesNothing` at the three names it expects hidden; `fetchGit.sh` at `[[ ! -e $path12/not-exported-file ]]` | `exportIgnoreFilterIsLeftOutWhenNoSourceNamesIt` passes under the mutation, since it asks for the identity where the identity is right; the decision's own pins are the `EXPECT_FALSE(hidesNothing)` lines of the declared-anywhere test, which the decision before this change failed for five sources (an uncommitted working-directory file, an index-only file, the XDG file set through libgit2's search path, a system-file macro, `core.attributesFile` with `~/`) while the filter, then always applied, hid the files: names passed through a filter that was not the identity |
| `ContentAddress::parse` refuses `fixed:git:sha1` | `ContentAddress.sha1GitFormIsReadable`, `LocalStoreObjectsTest.sha1GitRowIsReadVerifiedAndCollected` (at the row read the collector performs), `git-hashing/simple-sha1.sh` at the planted row's `nix path-info` | the pins of the tolerance pass on the old code too; the rejected design is what they discriminate |
| `makeFixedOutputPath` refuses SHA-1 under the git method | `DerivationAtermTest.sha1GitFixedOutputReads` | |
| the verifier expects a blob for every regular file | `LocalStoreObjectsTest.verifyExpectsNoBlobForAFileTheStoreDoesNotPlace` (a writable file is left as written and no blob is expected) | |
| `optimiseStore` does not count `rowsMigrated` | `LocalStoreObjectsTest.optimiseStoreMigratesAnOldRowAndEntersNoModifiedPath` | |
| `nix config show` no longer excludes the removed setting from its key=value dump | `optimise-store.sh` (`grepQuietInverse '^auto-optimise-store'`) | |
| the specific delete's note printed at `info` | `object-store.sh` with `nix store delete --quiet` | |
| the doors' write removed (`realise(Value&)` without `flushPendingWrites`) | `WriteBufferTest.aContextFreeLiteralNamingAPendingPathCannotEscape`, `realiseOfAContextFreeStringWritesEverythingPending`, `realiseWritesWhatTheContextNames`, `valuePrinterRealisesBeforeEmitting`, `ambiguousPrinterRealisesBeforeEmitting` | the C boundary tests pass, the boundary writing on its own |
| the dereference rule removed (`writePendingBeforeRead` returns at once) | `WriteBufferTest.aContextFreeLiteralNamingAPendingPathCanBeRead` | |
| the C boundary's write on the failure path removed | `nix_api_boundary_test.error_text_names_only_written_objects` | |
| `finish()`'s write removed | `WriteBufferTest.finishWritesEverythingPending` | |
| the C boundary's write on the returned path removed (both success-path writes of the one-phase boundary; either alone is covered by the other, since `nix_expr_eval_from_string` writes on its return and `nix_get_attr_name_byidx` on its) | `nix_api_boundary_test.an_attribute_name_names_only_written_objects` | `nix_get_string` is a door of its own, so a returned string is covered without the boundary |
| a `Value`-derived class reads the payload; the exec helper called without the token | the compiler refuses both (`static_assert(!ReadsBytes<Value>)` and the token's traits in `src/libexpr-tests/value/value.cc`; `execProgramInStore` and `execvpAfterEvaluation` take a `FinishedEvaluation` by value) | |
| a planted accessor call in `bundle.cc`, a C entry point outside the boundary, a handle-borne force, a K&R-braced entry point, an `execvp` in `CmdRun::run` | `maintainers/check-string-kernel.sh` reports each (its self-test, on every run) | |
| `HashingVisitor`'s file sink ignores `isExecutable` | `ObjectHashSink.prop_both_routes_match_reference_and_nar_length` (falsified on an executable root), `MerkleHash.objectHashCases` | the bare-root and empty-directory examples these two subsume were removed for them |
| `ObjectHash::render` writes the hash with its algorithm prefix | `ObjectHash.render_carries_the_hash_base16`, `parse_inverts_render` | |
| `merkle::objectHash` returns the entry's hash for every root | `MerkleHash.objectHashCases` | |
| the git accessor names no subtree (a `nullopt` for tree entries) | `GitUtilsTest.namesFollowTheTreeObjects` (`shared1.second` false), `GitUtils.prop_sharedSubtreesNameAndRenderAlike` | |
| `LocalStore::recordObjectHash` writes nothing | `LocalStoreObjectsTest.migrationDoesNotRootThePath` (a fresh store still reads the assertion), `ObjectHashLocalStore.migratePathInfo_migrates_an_old_row`, `read_only_store_does_not_write_the_migration` | |
| `narHashOf(Store &, path)` hashes nothing | `LocalStoreObjectsTest.addToStoreFromDumpUnderTheGitMethod` | |
| the `path` fetcher copies without naming first (`PathInputScheme::getAccessor`'s `isValidPath` of the name removed, the add unconditional) | `flakes/path-input-copies-once.sh` at its second evaluation (`grepQuietInverse 'copying .* to the store'`) | |
| the in-memory add's `autoGC()` moved back before the validity check (one call for both routes) | `LocalStoreAutoGCTest.inMemoryAddOfAValidPathRunsNoCollection` (`Actual: false`, `Expected: true`, "a collection ran for an add that wrote nothing") | the spilled route's call stays before its restore, whose outcome decides validity: `spilledAddToStoreFromDumpsOwnCollectionKeepsItsObjects` passes either way |
| `GitSourceAccessor::readDirectory` returns untyped entries again (`DirEntry{}` in place of `typeOfMode`) | `GitUtilsTest.readDirectoryReturnsTypedEntries` (`lookups` 5 where 0; each entry's `type` nullopt where the lookup's) | |
| the ATerm writer's store-directory refusal removed | `DerivationAtermTest.storeDirOutsideTheVerbatimFormIsRefused` (no `FormatError` from `unparse` for `/nix/st"ore`, `/nix/st\ore`), `WindowsStoreDirTest.unsupportedIsRefusedAtTheWriter` | the escaped mode's round trip passes either way |
| the collector sorts its dead set through the migrating query again (`topoSortPaths(visited)` in place of `topoSortPathsBy` over `queryPathInfoUnmigrated`) | `LocalStoreObjectsTest.deadSchema10RowIsDeletedWithoutAWalk` (`file '/fifo' has an unsupported type of fifo` thrown from `collectGarbage`) | |
| `checkSignatures` counts a key without verifying | `LocalStoreObjectsTest.versionOneSignatureOnARowVerifiesWithTheWalk` (1 where 0), `ObjectHashPathInfo.checkSignatures_accepts_v1_only_with_assertedNarHash` | |
| `checkSignatures` loses the content-addressed shortcut | `signing.sh` at `nix copy --to $TEST_ROOT/store0 $outPathCA` (line 130, the content-addressed copy without signatures; the later `nix store verify -r $outPathCA --sigs-needed 1000` pins the same shortcut) | |
| the version-4 JSON writer omits `narHash` for an asserted description | `ObjectHashPathInfoJsonV4.PathInfo_pure_asserted_to_json` | |
| the version-1–3 JSON reader drops `narHash` | `PathInfoTestV1/V2/V3.PathInfo_*_from_json` (master's goldens, whose expected values now carry `assertedNarHash`) | |
| the old wire form's reader discards the slot | `ObjectHashWorkerProto.write_without_feature_sends_the_nar_hash`, `read_without_feature_rejects_a_rendering_in_the_slot` | |
| the fixed-set filter keeps the set in the inner coordinates (no rebase) | `FetchToStore.prop_wrapped_dump_equals_filtered_dump` (`access to path '/' is forbidden`) | |
| `addPath` wraps a filtered `sha256` tree twice | `AddPathTest.unnamedFilteredTreeWithSha256IsWalkedOnce`, `namedFilteredTreeWithSha256IsWalkedOnceAndPaired` | |
| `recordTreeAddress` records nothing | `GitUtilsTest.narHashOfIsTheMemoisedShim` (`lookupTreeAddress` nullopt), `AddPathTest.namedFilteredTreeWithSha256IsWalkedOnceAndPaired` | |
| `writePendingBeforeRead` writes for every path, pending or not | `WriteBufferTest.aContextFreeLiteralNamingAPendingPathCanBeRead` (0 pending where 1), `coercionsWriteWhatTheyDereferenceAndNothingForTheRest` | the converse of the row above |
| `encodeAttrs` writes an integer as the string of its digits (the type tag dropped: `{"v": 1}` and `{"v": "1"}` one row) | `AttrsCodec.prop_encodeAttrs_decodeAttrs_roundTrip` ("Falsifiable after 26 tests and 1 shrink", the counterexample `int: 1`, an empty name), `AttrsCodec.prop_encodeAttrs_is_injective` ("Falsifiable after 34 tests and 2 shrinks"), `AttrsCodec.encodingIsPinned` (`"s0:0:b1:b1s1:n20:18446744073709551615s1:s3:x\0y"` where `"s0:0:b1:b1i1:n18446744073709551615;s1:s3:x\0y"`), `AttrsCodec.lazyAttrIsForcedAsAttrsToJSONForcesIt` (`"s1:x2:99"` where `"i1:x99;"`), `FetcherCacheTest.roundTripsAnEmptyNameAndNulBytesThroughTheDatabase` (`big` and `zero` read back as strings), `FetcherCacheTest.aKeyDifferingOnlyInTypeMisses` (`cache->lookup({"test", asInt})` `Actual: true`, `Expected: false`); and every reader of a memo row: `FetchToStoreTest.gitSubtreeMemoRespectsFilters`, `oldFormGitRowIsAMissAndTheRowIsTheEntry`, `FetchToStoreLocalTest.copyIsMadeFromTheObjectStoreWhereTheMemoNamesWhatItHolds`, `GitUtilsTest.memoRowsHoldEntryHashesForBareExecutableAndSymlinkRoots`, `exportIgnoreFilterIsLeftOutWhenNoSourceNamesIt` (each `input attribute 'mode' is not an integer`) | `AttrsCodec.truncatedAndMalformedEncodingsThrow` passes, the mutated writer's strings being well formed; a mutation of the decoder alone is caught by the round trip and the pinned vector, not by the injectivity property, which reads the encoder only |

**A fix carries a test that failed first.** Every code fix on this branch after the object store was built has a test that was run against the unfixed code and failed at a quoted assertion before it passed on the fixed tree; `07-defect-ledger.md`'s table names the test for each defect. Where a test and its fix share a build the before-run is made by reversing the fix's own hunks.

## 5. The two platforms

**macOS** (Apple Silicon, APFS, clang, per-file compilation; a writable home, Internet, the case hack on): the development platform. It alone can exercise the case hack (`Foo`/`foo` restored as `foo~nix~case~hack~1`, the route-independence check of `01` section 9.11), `.app/Contents` (files the store leaves as written), and APFS's costs, where a hard link is dearer than a small write, so every timing taken here is a direction only.

**aarch64-linux** (gcc, the unity build, sandboxed package builds with no network, ext4 over NVMe): the platform for constants and the gate before a step is done. Run as `nix build --system aarch64-linux .#nix-util-tests.tests.run` (and the other four unit packages) and `nix build --system aarch64-linux .#nix-functional-tests`. What it finds that macOS cannot: gcc's differences from clang (an explicit-object lambda clang accepts can crash gcc; `testing::HasSubstr` written inside `namespace nix` resolves to the test-support namespace `nix::testing` under gcc; a struct holding a reference under `NIX_SUPPORT_ACL` cannot be brace-initialised empty); the unity build's ambiguity between same-named file-local functions and same-named fixtures in two test files; the sandbox's absence of network, so a test that relies on substitution must pass `--option substitute true` on the command line (the CLI otherwise turns substitution off when it detects no Internet), and of the developer's caches, so a test that names a tree reads a fixture-owned `NIX_CACHE_HOME`; rapidcheck seeds that grow past a small generator's domain (a generator must draw from the domain directly, never ask for more distinct values than it holds); shared files at scale (`gc-auto.sh`, the `max-free` decision of `01` section 10); and the build hook's hand-off of every setting to its child, caught only by `cli-characterisation.sh`'s goldens, which compare stderr.

**The state of the tree, both platforms.** Linux: the five unit `tests.run` derivations build; `nix-functional-tests` reads `Ok 205, Fail 0, Skipped 33`, the eleven `local-overlay-store` tests among the skipped (section 7). macOS, after the thinning of the corpus (the second pins of laws and defects removed, each law's remaining pin shown to discriminate by a mutation of section 4): `nix-util-tests` 849 passed, 1 skipped; `nix-store-tests` 879 passed, 5 skipped (the HTTPS binary-cache tests); `nix-fetchers-tests` 106 passed, 1 skipped (the gated real-store check); `nix-expr-tests` 426; `nix-flake-tests` 29; `--suite git-hashing` Ok 6; the named main tests with `NIX_REFERENCE_BIN` set Ok 23, Skipped 2 (`local-overlay-store:gc`, a homonym meson adds; `db-migration`, which runs only with `NIX_DAEMON_PACKAGE`); `--suite ca` Ok 25, Skipped 1; `--suite flakes` Ok 35. A checkout whose path contains `@` fails `ca/repl.sh`, `ca/nix-shell.sh` and `flakes/absolute-paths.sh` on a parse error in the tests' own expressions (`simple = import /…/user@host/…`); an environment limit, absent from a checkout path without one. The counts are what the binaries printed at the tree's head and move with every test added; the commands above reproduce them.

## 6. Measurements, with their commands

Constants are Linux's; macOS timings are dominated by the machine's per-open cost, are directions only, and are not quoted here.

**The object store on Linux** (`runs/linux-measure-object-store.nix`, its driver `runs/linux-timing-object-store.sh`; `nix build --impure --no-link --print-out-paths -L -f runs/linux-measure-object-store.nix --argstr repo <checkout> --argstr branchRev <rev> --argstr masterRev <rev>` on an aarch64-linux builder; a generated repository of 40 directories and N files of about a dozen bytes; a fresh store root primed with the clean commit, then three one-file edits of a dirty checkout through `nix eval --raw` of `fetchGit`'s `outPath`; separately two tarballs differing in one file into a fresh store and tarball cache per trial; master run with `auto-optimise-store` off and on, the branch having no setting; Linux 6.17, ext4 over NVMe, a shared builder at load 3 to 4). Seconds; the branch measured in two rounds:

| N files | binary | prime, whole tree | edits 1, 2, 3 |
|---|---|---|---|
| 4000 | branch | 0.28; 0.29 | 0.12 0.14 0.12; 0.13 0.13 0.12 |
| 4000 | master, sharing off | 0.18 | 0.15 0.15 0.18 |
| 4000 | master, sharing on | 0.25; 0.22 | 0.29 0.28 0.27; 0.28 0.25 0.27 |
| 40000 | branch | 2.64; 2.58 | 1.06 1.01 1.06; 1.08 1.05 1.06 |
| 40000 | master, sharing off | 1.36 | 1.37 1.27 1.29 |
| 40000 | master, sharing on | 1.76; 1.79 | 2.73 2.40 2.61; 2.68 2.58 2.47 |

| N files | binary | first fetch | second fetch, one file differing |
|---|---|---|---|
| 4000 | branch | 0.28 0.27 0.25; 0.26 0.27 0.25 | 0.11 0.15 0.13; 0.11 0.12 0.13 |
| 4000 | master, sharing off | 0.21 0.23 0.23 | 0.18 0.17 0.15 |
| 4000 | master, sharing on | 0.27 0.26 0.25 | 0.30 0.30 0.31 |
| 40000 | branch | 2.30 2.28 2.07; 2.28 2.19 2.25 | 0.79 0.73 0.72; 0.75 0.79 0.78 |
| 40000 | master, sharing off | 1.35 1.31 1.35 | 1.13 1.15 1.28 |
| 40000 | master, sharing on | 1.82 1.83 1.86 | 2.60 2.67 2.47 |

Reading, direction only: on the operations the object store exists for — the second fetch of a tree differing in one file, the copy of an edited checkout — the branch is faster than master with sharing on by about 2.4 to 3.4 times and than master with sharing off by about 1.25 to 1.5 times at 40000 files [derived from the rows]; master with sharing on is slower on its second fetch than on its first, its post-write optimisation pass re-hashing and re-linking every file, which the branch's decision before the write removes. Two facts the cost paragraphs of `01` must be read against: the branch's *first* entry of a tree is slower than master's (2.6 s against 1.4 and 1.8 at 40000 — the SHA-256 of every blob, the object store's file and its link, paid once per new blob); and the branch's edited copy is proportional to the number of files, not to the change — about 40 percent of a whole-tree entry for one file changed in 40000 — because the one route dumps and restores the whole NAR and makes one link per duplicate file (`01` section 9.10, "One route"; `01` section 13). The store afterwards, N=4000: the branch holds 12044 regular files under the store root, 12001 with two or more links; master with sharing on 12001, all linked through `.links`; master with sharing off 8000, none linked.

**The copy from the store's own objects on Linux** (`01` section 9.10, *One route*, the second route; the design report's method — `scratchpad/mat/measure/linux-phases.nix` and its script `phases.sh`, an aarch64-linux derivation on the remote builder, `nix build --impure --no-link --print-out-paths -L -f linux-phases.nix` with `BEFORE_REV` and `AFTER_REV` in the environment; a generated repository of 40 directories and N files of about a dozen bytes, one commit; a fresh store root primed with the clean commit, then three one-file edits of the dirty checkout under `dir7`, each timed as `nix eval --raw` of `builtins.fetchGit`'s `.treeHash` (the naming alone), `.outPath` (naming + copy + registration) and `.outPath` again (the memo hit); a fourth edit's copy under `strace -f -c -w` for the syscall counts; before = `c7d00fdc3`, after = `60e429c4a`; Linux 6.17 aarch64, ext4 over NVMe, 20 CPUs, load 2.7 to 3.6 from other users throughout; output `/nix/store/kcjfqmcpn3hh928jr4ra22kmk727aygj-c2-linux-phases`, log `phases.log`). Seconds, direction only:

| N files | binary | prime full, clean | edits 1, 2, 3: name-only | edits 1, 2, 3: full | edits 1, 2, 3: hot |
|---|---|---|---|---|---|
| 4000 | before | 0.17 | 0.04 0.03 0.04 | 0.13 0.10 0.10 | 0.03 0.04 0.03 |
| 4000 | after | 0.17 | 0.03 0.03 0.03 | 0.07 0.07 0.07 | 0.03 0.03 0.03 |
| 40000 | before | 1.23 | 0.16 0.14 0.15 | 0.77 0.79 0.77 | 0.14 0.14 0.14 |
| 40000 | after | 1.19 | 0.17 0.15 0.15 | 0.51 0.55 0.51 | 0.16 0.14 0.13 |

Syscalls per unchanged file on the fourth edit's copy (`strace -f -c -w`; calls with at least N/2 occurrences, divided by N; the one `newfstatat` beyond the blob's that every route pays is libgit2's status `lstat` per tracked file, and the third before the change the traversal's `lstat` of the source file):

| N | binary | `newfstatat` | `linkat` | `openat2` | `pread64` | `fstat` | `close` | all calls |
|---|---|---|---|---|---|---|---|---|
| 40000 | before | 3.01 | 1.00 | 1.00 | 1.00 | 1.01 | 1.01 | 8.09 |
| 40000 | after | 2.04 | 1.00 | 0 | 0 | 0 | 0 | 3.21 |
| 4000 | before | 3.12 | 1.00 | 1.03 | 1.01 | 1.06 | 1.08 | 8.85 |
| 4000 | after | 2.14 | 1.00 | 0 | 0 | 0 | 0 | 3.97 |

Reading, direction only: the copy of the edited checkout (full minus hot) falls from about 0.63 to 0.37 s at 40000 files and from about 0.08 to 0.04 s at 4000 [derived from the rows]; its syscalls per unchanged file from about 8 to 3, the read (`openat2`, `fstat`, `pread64`, `close`), the NAR framing and the source `lstat` gone, the blob's `fstatat` and the `linkat` left, as `01` section 9.10 states. The first entry of the tree (`prime full`), the naming and the memo hit are unchanged within the run's noise (the one memo lookup per directory the route adds is 41 lookups here). In every run the copied path dumps to the checkout's bytes and every regular file of it has a link count of at least two (both checked by the script). Before this change the copy paid 8 syscalls per unchanged file, not the 18 the design report measured: the export-ignore identity of `01` section 9.9 had already removed libgit2's attribute lookups. What remains is `cp -al`'s floor (the design report: 0.28 s at 40000 on the same builder) plus the status `lstat` and the process. **macOS, once, direction only** (`phases-macos.sh` beside the Linux script; this machine, Darwin 25.6.0 arm64, APFS, the endpoint-security agent of the design report active, load 2.4 to 11 during the run; the CLIs built by `nix build` of the two revisions; one edit per N): at 40000 files the edited copy (`.outPath`) took 84.24 s before and 12.17 s after, the memo hit 0.25 and 0.21 s, the first entry 213 and 211 s; at 4000 files 1.71 and 1.46 s (the first entry 8.57 and 3.05 s — the 4000-file rows are inside this machine's noise). The route opens no source file, which on this machine is the cost (design report §1.3), so the copy's remaining time is the `linkat` per file; the copied path dumped to the checkout's bytes and every file was a link in all four runs.

**The memory bound of the write buffer** (`tests/functional/differential/measure/measure-eval-linux.sh` as a derivation on the Linux builder; GNU `time -v` peak resident set; N independent derivations). The *eager* rows were taken before the boolean deferral setting went (`01` section 10, *Deferral is unconditional*), with it off — the reference's behaviour; the script now takes the binary to measure, and a re-run makes the eager rows with the reference binary and the deferred rows with the branch's:

| command | mode | N=1000 | N=4000 | N=16000 |
|---|---|---|---|---|
| `nix eval --json` | eager | 41 MB | 51 MB | 90 MB |
| `nix eval --json` | deferred, unbounded | 52 MB | 82 MB | 196 MB |
| `nix-instantiate` | eager | — | — | 88 MB |
| `nix-instantiate` | deferred | — | — | 88 MB |

Unbounded, the deferred-minus-eager gap for `nix eval --json` is 11, 31 and 106 MB (the eager N=4000 run printed 51 MB once and 52 MB once; 51 is used in both tables): linear in the pending objects, about 7 KB each (the `ValidPathInfo`, the map node, the allocator — an order of magnitude more than the derivation text, which is why a byte cap never fired and the cap is a count). `nix-instantiate` and the plain printer realise each string as they emit it and are flat. With `deferred-store-writes-max-pending` at its default of 4096:

| N | eager | unbounded | cap 4096 | flushes at 4096 |
|---|---|---|---|---|
| 4000 | 51 MB | 82 MB | 82 MB | 1 |
| 16000 | 89 MB | 196 MB | 141 MB | 4 |
| 32000 | 135 MB | — | 179 MB | 8 |
| 64000 | 230 MB | ~680 MB (extrapolated) | ~300 MB | 16 |

At N=16000 the setting sets the flush count and the peak directly: 1024 → 16 flushes, 120 MB; 4096 → 4, 141 MB; 16000 → 1, 200 MB. (C2) holds throughout because the accumulating sinks emit nothing until after the final realise (`WriteBufferTest.autoFlushKeepsReferencesValidForLaterReferrers`).

**Write and round-trip counts** (`measure-writes.sh`, `count-ops.sh`; deterministic): the `.drv` closure a command writes has the same number of objects eagerly and deferred at every size; a closure reached from one coercion is one flush (`closure`, N=2000: 2001 writes eager, 1 flush deferred with 2001 pending), independent top-level derivations printed one by one are one flush each (`fan`: 2000 writes, 2000 flushes, peak pending 1). Through a daemon a ten-object closure costs 11 `AddToStore` and 11 `AddTempRoot` eagerly, and 1 `AddMultipleToStore`, 1 `QueryValidPaths` and 11 `AddTempRoot` deferred: the protocol has no bulk temp-root operation (`AddTempRoots` is reserved and unimplemented in `worker-protocol.hh`), so the roots are one round trip per object either way.

**The export-ignore filter's lookups** (`01` section 9.9, *the filter is the identity when the repository declares nothing*; the derivation `measure.nix` and its driver `measure.sh` of the change's scratch notes, the pattern of the object-store measurement above: `nix build --impure --no-link --print-out-paths -L -f measure.nix --argstr afterNix <nix-cli> --argstr beforeNix <nix-cli>` on the aarch64-linux builder, load 0.8-1.0 on 20 cpus). The nixpkgs tree `b1b8759…` copied, committed and repacked inside the sandbox — a repository *with a working directory*, 38 258 tree directories, one `.gitattributes`, no line naming `export-ignore` — then `hello.name` from `builtins.fetchGit { url = "file://…"; rev; shallow = true; }`, `--impure --expr`, a fresh store and cache per binary; wall by `date`, syscalls by `strace -f -c -w` (all threads, wall-clock), before = `3c1485628`, after = the identity short-circuit:

| `hello.name` via | wall, warm (3 runs) | traced syscalls, warm (3 runs) | of which | cold, run 1 (names the tree) |
|---|---|---|---|---|
| `fetchGit { rev; }`, before | 0.47, 0.47, 0.48 s | 364 514 / 364 522 / 364 512 | 180 552 `newfstatat`, 157 725 `readlinkat`, 22 681 `faccessat`: the per-lookup probes | 13.51 s |
| `fetchGit { rev; }`, after | 0.25, 0.24, 0.25 s | 41 978 / 41 978 / 41 978 | 38 443 `newfstatat`: one `stat` of `<dir>/.gitattributes` per tree directory, the decision's price for a checkout | 2.33 s |
| `fetchGit { rev; exportIgnore = false; }`, either | 0.08-0.11 s | 3 684 | 1 704 `openat2` (the union root's probes) | 0.10-0.11 s, a memo hit on the filtered naming's rows: the same tree name |

Reading, direction only: for a checkout the filter's warm cost falls from about 0.37 s and 361 000 syscalls to about 0.15 s and 38 000, the remainder being the stats above and the tree walk that lists the directories, once per process; a bare repository — the cache clone of a remote URL — has no working directory to probe and pays neither. Cold naming under the default falls to the unfiltered naming's order. A lazy variant that probes only the directories an evaluation visits would take the warm case to the floor at the price of keeping the wrapper and its name pass-through; not built.

**The memo's writes at a cold naming** (macOS, this machine, direction only; the change's scratch driver `c1-measure.sh`: the 40 000-file repository of the object-store measurement above, committed; `nix eval --raw --impure --expr '(builtins.fetchGit { url; rev; }).treeHash'` — the naming walk alone, no copy — against a warm clone in the fetchers' `gitv3` cache and a warm cache file, the `sourcePathToHash` rows deleted before each run; three runs each, load 3–5 from other work). As the code stands, one `upsert` per node as it is hashed (`MemoVisitor::record`): 2.05, 2.04, 2.06 s, 40 041 rows written. With the `upsert` a no-op: 1.63, 1.30, 1.42 s. With the rows held by the visitor and written in one `SQLiteTxn` after the walk (built for this measurement and not kept): 3.00, 1.94, 2.02 s. So the 40 000 statements are about a third of the cold naming, and the transaction is not where it goes: a sampling profile of the batched write (`sample`, 1 ms) put about three quarters of the flush in `attrsToJSON(…).dump()` of the key and the value — the cache's JSON-text representation, encoded once per row here and once more per lookup — and about a fifth in `sqlite3_step` (`sqlite3BtreeInsert`); the per-statement commit under `synchronous = off` in WAL mode is the rest, and one transaction saves only that. The representation is now the cache's own: every row's key and value is `encodeAttrs` of its `Attrs` (`attrs.cc`; a type tag, length-prefixed name and bytes, decimal integers, one codec for every domain), in `fetcher-cache-v5.sqlite`; the JSON text is gone from the cache and `SQLiteStmt::Use::getStr` reads a column by its length, so a NUL byte survives the round trip (`FetcherCacheTest.roundTripsAnEmptyNameAndNulBytesThroughTheDatabase`). Measured with the same driver parametrised by binary and cache file, the tree of the base commit built beside the change (load 2.5 to 4.4): cold naming, rows deleted before each run — before 2.05, 2.03, 2.11 s; after 1.58, 1.69, 1.67 s; 40 041 rows written by either — so about 0.4 s of the 2.06 [derived: the means 2.06 and 1.65], a fifth of the cold naming and two thirds of what the `upsert` stub saved [derived: 2.06 − 1.45 = 0.61 against 0.41], went to the JSON. Warm naming, rows present — 0.15, 0.15, 0.15 s before and 0.15, 0.15, 0.15 s after — reads one `sourcePathToHash` row, the root's (`-vvv`: one `using cache entry` for the domain and no miss; the cold run prints 40 042 misses before its 40 041 writes), so the read side of the memo is exercised by the cold walk's misses and not by a warm one; a copy of the repository with a `.gitattributes` declaring `export-ignore` on a path that does not exist, which keeps the filter, is also one hit, 0.14 s either way. A sampling profile of a cold run after the change (`sample`, 1 ms; 614 main-thread samples): `MemoVisitor::record` 221, of which `CacheImpl::upsert` 177 — `sqlite3_step` 151 (`sqlite3PagerCommitPhaseOne` 69 and `pwrite` 88 beneath it, the per-statement commit) and `encodeAttrs` 21; `MemoVisitor::known` 153, of which `CacheImpl::lookup` 75 — `sqlite3_step` 56, `encodeAttrs` 14 — and the key's composition (`getFingerprint` 38, `makeSourcePathToHashCacheKey` 21). The codec is 35 samples of the 614 [derived: about 6%]; what remains of the memo's cost is SQLite's statement and commit, about a third of the naming [derived: 207 of 614].

**The collector on a dead schema-10 row** (macOS, this machine, direction only; the semantics run's driver `e5-gc-cost.sh` against `build/src/nix`: a fresh store root, an 800 MB file of random bytes added by `nix store add --mode nar` and left unrooted, its row rewritten to the schema-10 form — `sqlite3 <state>/db/db.sqlite "update ValidPaths set hash = 'sha256:<base16 NAR hash>' where path = '<path>'"` — then `/usr/bin/time -p nix-store --gc`; four runs alternating a current row and a planted one). Before the collector's non-migrating read (`queryPathInfoUnmigrated`, `topoSortPathsBy`): current row 0.15, 0.15 s; planted row 0.71, 0.71 s — the difference is one read of the 800 MB and its two hashes (`walkOldRow`) before the deletion. After: current row 0.14, 0.14 s; planted row 0.15, 0.14 s.

**The one-address bijection** is checked byte-exact over real store objects: `GitUtilsTest.storeObjectsRoundTripThroughAGitTree` and `realStorePathsRoundTripThroughAGitTree` sample up to `maxToCheck = 300` objects of the machine's store (`src/libfetchers-tests/git-utils.cc`), run on macOS and on aarch64-linux; the second opens the machine's own store, so it runs only under `NIX_TEST_REAL_STORE=1` and the default run skips it, saying so — the 600-object figure is from gated runs.

## 7. Owed, and why

- **The `local-overlay-store` suite.** Its eleven tests need a privileged overlayfs mount and skip on macOS and inside the Linux sandbox; they have run on neither platform on this branch. The overlay's object-store guard — `LocalStore::ownsObjectStore()` false for `LocalOverlayStore`, so no sweep, no `.links` removal and no object check through the merged store directory (`08` K13) — is by reading. Owed: a sweep over the upper layer alone, and the suite on a host with the mount (*Overlay.upperLayerSweepReclaimsUpperObjectsOnly* below).
- **`ENOSPC` on a full directory index.** A file `place` could not enter leaves no mark, so `placementOf` cannot know it; the verifier reports the file as missing and `--repair` retries (`01` section 9.10, law 3's fourth exception). Unverifiable without a full directory index; stated, not tested (*Migration.enospcLeftFileIsReportedNotRepairedForever* below).
- **The concurrent window X1** (`08` section 1.5). A collector in another process can take an in-flight ingestion's trees between its restore and its registration; the path is then registered naming a tree the store lacks, reported by every collection and by `--verify --check-contents`, re-entered by `--optimise` (`01` section 9.10, law 4's second false-by-design case; law 5 sweeps on). Accepted and stated; the hook-driven interleaving test is owed (*LocalStore.ingestionTreesSurviveAConcurrentSweep* below); its single-path forms are pinned (`LocalStoreObjectsTest.sweepAndVerifyOnALiveRootWhoseTreeIsMissing`, `sweepTolerantOfAnUnenteredRoot`, `LocalStoreAutoGCTest.*`).
- **Windows.** The object store shares nothing there; by inspection only, no Windows build or run (`01` section 13).
- **Two facts stated without a discriminating test**: the filter over the children of a case-hacked *directory* name descends by the on-disk name as `dumpPath` does (reachable only on the case-hack platform after two directory names collided), and `objectHashOf` refuses a tree deeper than `narMaxDepth` as `dumpPath` does. Both hold by construction now that there is one traversal (`src/libutil/include/nix/util/tree-traversal.hh`).
**The tests owed**, each a test that does not exist, its name a proposal in italics so that no check mistakes it for a test in the tree, and what it would pin. A row leaves this list when its test lands and `07-defect-ledger.md`'s table names it.

| proposed test | what it would pin |
|---|---|
| *Interop.newClientAgainstOldDaemon_pathAndFileInputs* (functional; a reference daemon from `NIX_REFERENCE_BIN`, a branch client) | `fetchTree {type="path"}`, `fetchTree {type="file"}` and a flake with a `path:` input evaluate and copy against an old daemon (the `object-hash.sh` block covers `builtins.path`, a `file` input, the write buffer and the importer; the flake-input and `path`-type cases are not driven) |
| *Signatures.copyOfCacheSubstitutedPathToRequireSigsStore* (functional; two stores, an untrusted daemon user) | `nix copy` of a path substituted from a NarHash-only cache to a `require-sigs` daemon succeeds (the unit form is `LocalStoreObjectsTest.versionOneSignatureWithoutAssertedNarHashIsCheckedOnTheStream`; the daemon-to-daemon copy is not driven) |
| *GcCost.deleteSpecificReadsOnlyTheDeletedPathsTrees* (unit; counting hooks on `readTree` and the blob `lstat`) | `nix store delete` reads O(that path's trees), not the store's — today it sweeps nothing and reads nothing; the test would pin that a future per-path sweep stays proportional |
| *ObjectStore.lawFourHoldsForEveryValidPath* (unit) | for every valid path, every tree its hash reaches and every blob a reached tree names is present, across a hook-driven interleaving of an ingestion and a sweep (the per-path form is `verifyObjects`, pinned by `verifyExpectsNoBlobForAFileTheStoreDoesNotPlace` and `sweepAndVerifyOnALiveRootWhoseTreeIsMissing`; the interleaving form is owed) |
| *ObjectStore.writeObjectFileIsCanonicalBeforeFsync* (unit; a hook at the fsync) | mode 0444 or 0555 and mtime 1 at the moment of the object file's fsync (the observable is what survives a crash; no hook exists) |
| *LocalStore.ingestionTreesSurviveAConcurrentSweep* (unit; two threads or a hook-driven interleaving) | the specification says an in-flight ingestion's trees need not survive a concurrent sweep (`01` section 9.10, "No grace"); the test would pin the stated consequence — the path registered, reported, re-entered by `--optimise` — under the race itself. The ingestion's own collection is pinned (`LocalStoreAutoGCTest.*`) |
| *Collector.deletesEverythingNotAllowListedInTheStoreDir* (unit) | plant `.objects`, `.links` and `foo` in the store directory, collect, and assert exactly the allow-list survives |
| *Collector.tempRootDuringCollectionGoesThroughTheSocket* (unit; an in-process collection with a hook at `pending`) | a root registered mid-collection keeps its path, and the migration registers none (`migrationDoesNotRootThePath` pins the second half without the socket) |
| *RestoreSink.derivedSinkSeesEveryLevel* (unit) | a `FileSystemObjectSink` composed over `RestoreSink` receives `createRegularFile` for a file two directories down (the copy visitor's form is `CopyRecursive.copies_a_tree_under_both_sink_conventions`; a generic composition over `RestoreSink` is not driven) |
| *Merkle.memoRowIsTheEntryHashForEveryRootKind* (property over generated trees with a random root kind) | the row written for any root equals what the parent's walk reads for that node (the example form is `GitUtilsTest.memoRowsHoldEntryHashesForBareExecutableAndSymlinkRoots`) |
| *NarParser.caseHackSuffixRefusedAndNeverEmittedOnEveryPlatform* (unit; `use-case-hack` on and off × dump and parse) | the four cells agree: a name carrying the suffix is neither emitted nor accepted (`NarTest.entryNameWithCaseHackSuffixIsRefusedUnderEitherSetting` and `MerkleHash.unhackNameAndUnhackedEntries` cover three) |
| *Enumerate.everyBuildModeEntersWithItsRepairFlag* (unit over `bmNormal`, `bmRepair`, `bmCheck`) | the flag passed to `enterPath` is the mode's (`repairRelinksAFreshFileOverACorruptBlob` pins `bmRepair` alone) |
| *Enumerate.everyPathInfoReaderAcceptsBothColumnRenderings* (unit over `--dump-db`, `--load-db`, JSON 1–4, narinfo, worker, serve, the database row) | each reader accepts `git:sha256:` and `sha256:` where it must (the per-form tests exist; the enumeration over all readers does not) |
| *Enumerate.everyDescriptionKindCanBeSigned* (unit over object-hash-only, NAR-hash-only, both, neither) | `nix store sign` signs the first three and refuses the fourth with the stated message (`ObjectHashPathInfo.signV1_without_objectHash_does_not_throw` covers one cell) |
| *Enumerate.everyConsumerOfACacheAttribute* (a list of the readers of each fetcher-cache row kind, checked against `grep -rn`) | no reader of `treeHash`, `sourcePathToHash`, `treeAddress`, `treeMentionsAttribute` rows is missing from the list |
| *Enumerate.everyOutputModeCopiesBeforeEmitting* (`nix eval` plain, `--json`, `--raw`, `--write-to`; `nix-instantiate --eval`, `--xml`, `--json`, `--raw`; the (C2) observer) | in every mode a mounted input is in the store before its name is emitted (the observer covers `nix eval`'s three; `--write-to` and `nix-instantiate`'s modes are not driven) |
| *Fs.singleFileRootDestinationHasNoTrailingSlash* (unit) | the destination computed for `CanonPath::root` equals the target exactly (`p / ""` appends a separator; the restore of a one-file NAR at its destination is exercised inside `LocalStoreObjectsTest.restoreThroughObjects`) |
| *ExportIgnore.untrackedGitattributesInWorkdir* (unit) | an untracked `.gitattributes` naming a filter attribute withdraws the commit's names from the working-directory accessor (`GitUtilsTest.workdirNamesCleanSubtreesByTheCommit` covers `* text=auto`; a filter attribute is not driven) |
| *Signatures.oldStoreGitExecutableRootImports* (functional, reference binary) | a git-addressed path with an executable-file root made by master is refused on copy with a message naming the address change (confirmed by experiment; stated in the release note; not pinned) |
| *Overlay.upperLayerSweepReclaimsUpperObjectsOnly* (functional, Linux, the `local-overlay-store` suite) | a whole-store collection on the overlay removes the upper layer's unreachable objects and touches no lower object (no whiteout under `.objects`, no `.links` entry whited out); `--verify --check-contents` reports no lower path; the guard `LocalStore::ownsObjectStore()` is replaced by that sweep |
| *Migration.enospcLeftFileIsReportedNotRepairedForever* (unit; a full directory index planted or simulated) | a file `place` could not enter for `ENOSPC` is reported by the verifier once and `--repair` retries without error once the index has room |

## 8. Rules for changing this code

Each rule names what enforces it today; what only review holds is said to be held by review. The rules that sections 2 to 5 already enforce — one suite per command line, a fix's test run against the unfixed code first, a mutation restored by an edit that changes the mtime, both platforms before a step is done, the environment facts a test asserts — are not repeated; the call-site questions are `08-store-model.md` section 2.

1. **A finite domain is enumerated from the lowest primitive, in code.** Before writing "for all X, P(X)" where X is listable — builtins, output modes, enum cases, protocol directions, description kinds, the readers of a column, the callers of a primitive — write the check that iterates the set from its source of truth and run it; enumerate the callers of the primitive that produces the value, not of the wrapper in hand, since a wrapper can be inlined at a call site a grep for the wrapper misses; grep a call's *name* and read every hit, since a one-line shape of its argument list misses a call split over lines. For an infinite domain, the generator must be shown to produce the case just added and to reach the sizes claimed. *Enforced by:* the templates `WriteBufferTest.everyBuiltinAgreesWithEagerEvaluationOnPendingInputs` (`src/libexpr-tests/write-buffer.cc`) and `LocalStoreObjectsTest.gitMethodRefusesSha1AtEveryCreationEntry`; the compiler, for a changed signature (the type enumerates the sites, which is why `ObjectHash` is a strong type and `narHash` a field that no longer exists); review, for a changed contract whose sites the compiler cannot see.

2. **Both protocol directions, always, with and without the feature.** A wire or format change is tested old→new and new→old with a binary built from master, with and without the feature or version the old side would need. *Enforced by:* `tests/functional/object-hash.sh`'s interop blocks under `NIX_REFERENCE_BIN` (section 2); `git-hashing/simple-sha1.sh`'s reference-binary block.

3. **Enumerate the readers before refusing a form.** A refusal at a parser or a name function is preceded by a list of every reader that parses the form — in particular what the garbage collector and `nix-store --verify` read on every row and every derivation they visit; a reader tolerates, a creator refuses. *Enforced by:* `ContentAddress.sha1GitFormIsReadable`, `DerivationAtermTest.sha1GitFixedOutputReads`, `LocalStoreObjectsTest.sha1GitRowIsReadVerifiedAndCollected`, each shown to fail under the refusing design by mutation (section 4).

4. **A concurrency claim gets an interleaving test, or the law says "unchecked".** A sentence of the form "X cannot happen while the collector runs" or "the link count keeps it" is accompanied by a hook-driven interleaving unit test, or the law states that its concurrent form is unchecked and names the owed test. *Enforced by:* `01` section 9.10 law 5, which says so and names *LocalStore.ingestionTreesSurviveAConcurrentSweep* (section 7); the single-path forms pinned by `LocalStoreObjectsTest.sweepAndVerifyOnALiveRootWhoseTreeIsMissing`, `sweepTolerantOfAnUnenteredRoot` and `LocalStoreAutoGCTest.*`.

5. **A design decision carries its consequence list before the code.** Before a decision is built, the forms, peers, memos, stores and costs it changes are enumerated — the *stores* a rule governs (an upgraded store, a `--load-db` store, a `min-free` store), not only the case that raised it — and each consequence gets a check or an explicit "accepted, unchecked". *Enforced by:* `01` section 10, where each decision states its consequence; review, which refuses an entry without one.

6. **Correct by construction where the language can hold the boundary; a check that runs without judgment where it cannot; stated where neither reaches.** The compiler on a type's representation (a value's bytes private to `Value`; a strong `ObjectHash`; a move-only `FinishedEvaluation` the exec helpers demand); a check script for what C++ cannot scope (`maintainers/check-string-kernel.sh`, which plants five violations on every run and requires each reported); review for the rest, named as such (`04` section 0.2, "What is not proved"). *Enforced by:* the compiler, the hook, and the "Residue, stated" paragraph of `04` section 1.2.

7. **Counts come from artefacts; measure before explaining.** A number in these documents is pasted from a tool's output with the command beside it, or written with its arithmetic and marked `[derived]`; a number with neither has not been checked and says so. A performance sentence names its measurement; a causal explanation without one is marked `[hypothesis]`; a fix is placed where an experiment located the cause, not where a reading placed it. *Enforced by:* the convention of `README.md`; section 6 carries the command for every table; review.